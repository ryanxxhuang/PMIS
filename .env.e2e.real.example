# W6-1：只填臨時／隔離的 Supabase staging，正式專案 URL 會被設定檔拒絕。
E2E_REAL_SUPABASE_URL=https://<staging-project-ref>.supabase.co
E2E_REAL_SUPABASE_ANON_KEY=<staging-publishable-key>
# W6-2 起的鏈測試建立/清理 fixture 帳號用;只填 staging 的 key
E2E_REAL_SERVICE_ROLE_KEY=<staging-service-role-key>
# 全部測試自行建立並清理臨時帳號,不需常駐 smoke 帳密。
# (選填)live Edge 驗收用:填了 chain3 就走 live AI 模式(見 docs/REAL_BACKEND_E2E.md)
# ANTHROPIC_API_KEY=
# 視覺 stub(chain 5／6／8／12／14 要):值與 e2e-real/stub.env 相同。
# 為什麼也寫在這裡——`supabase functions serve` 只吃一個 --env-file:只給 stub.env 就沒有模型金鑰,
# chain3 的 live 抽取會失敗(「AI 服務尚未完成設定(代碼 config)」);只給金鑰就沒有 stub,照片鏈起不了稿。
# 兩組寫在同一個檔,一次 serve 就能把整套鏈跑完。stub 只在本機 http 位址生效(_shared/visionStub.ts)。
PMIS_VISION_STUB=1
PMIS_VISION_STUB_HINT=結構工程
