# 專案盤點報告 — 2026-07-09

> 全面盤點：本地/GitHub/部署、前後端、UI/UX、三方角色功能缺口。
> 實測方法：跑全測試套件、檢查遠端與線上部署、preview 以施工品管/機關兩角色實測（含深色模式與手機版）。

---

## 一、本地 / GitHub / 部署：健康，全部同步

**本地**：working tree 乾淨，`main` 與 `origin/main` 同步，無未推送 commit。
殘留本地分支 `claude/youthful-faraday-e71307` 可刪。`發包圖說20200715`（668MB）已 gitignore。

**GitHub**：CI（test+build）最近 5 次全綠。

**部署**（GitHub Pages 雙軌，2026-07-09 12:13 部署，晚於 main 最新 commit → 線上=最新碼）：
- 正式版 `https://ryanxxhuang.github.io/PMIS/`（Supabase 打包）HTTP 200
- 銷售 demo `https://ryanxxhuang.github.io/PMIS/demo/` HTTP 200

注意：部署是手動 `npm run deploy`，CI 不自動部署；`dist/_redirects` 是 Netlify 殘留（無害）。

**行銷官網**（`/Volumes/GameSSD/Projects/pmis-site`）最後 commit 7/4，早於全站 UI 改版
→ 產品截圖是舊 UI，待更新。

---

## 二、前端：功能完整，架構債複利中

React 18 + Vite 5 + Tailwind v4，25 條路由、13 個純邏輯 lib 模組，**91 tests 全綠**
（SCOPE.md 還寫 37，文件過時）。

主要問題：
1. **store.jsx 單一 context 已 1,601 行**（SCOPE 標記時 1,000+），每寫入全 app rerender，
   每加模組惡化。→ 按領域拆 slices。
2. Quality.jsx 518 行，查驗/缺失/自主檢查/取樣四塊擠一頁，可拆組件。

---

## 三、後端：功能全通，一個真正的安全缺口

Supabase Pro：idempotent schema（711 行）、全表 RLS、5 個 Edge Functions 全跑 Claude API
（Haiku 4.5 視覺/Sonnet 5 長文件），每日台北 08:00 提醒信實發驗證過。
第二波 AI（監造報表/風險稽核/AI 助理）= 前端確定性規則引擎，不燒 API 費。

**關鍵缺口：伺服器端 RBAC 未做。** 所有業務表 policy 都是 `members_all`——
任何專案成員（不分施工/監造/機關）都能直接寫任何資料。「監造才能核定、機關唯讀」
只在 UI 層 `can`。多組織試用前的硬性前置。

第二前置：Resend 網域驗證未完成（網域購買擱置中，專案名稱未定），
提醒信只能寄到帳號本人信箱。

SCOPE.md 多處過時（「2 個 Edge Functions(gpt-4o)」、37 tests、未含 AI 兩波）。

---

## 四、UI/UX：整體水準高，實測抓到的可修點

好的：深色模式全頁一致、手機 RWD 正常、角色化側欄正確、狀態徽章/ball-in-court 清楚。

按嚴重度：
1. **Dashboard 底部統計磁貼中文標籤直排**（「估驗期數」被擠成一字一行）
2. **進度頁 KPI 卡說明文字截斷**（「依預定 S 曲…」「累計估驗 ÷ …」）
3. **刪除專案太容易**：專案切換下拉裡一個 `window.confirm` 就永久刪整案
   → 改輸入專案名稱確認
4. **原生 confirm/alert 與精緻 UI 不搭** → 共用 Dialog 組件
5. **Demo 模式 reload 即登出**（demo 角色不持久化，簡報時 F5 中斷 storyline）

---

## 五、三方角色功能缺口

**施工廠商（最完整）**：全鏈路閉環。缺：分包商 Commitments/對帳、試驗管理通用框架
（現只有混凝土試體齡期）。

**監造（第二完整）**：查驗/送審/RFI/AI 監造報表都有。缺：**ITP 檢驗停留點（W/H/R）**
——SCOPE 附錄 A 標為結構關鍵未實作，是查驗申請上游、監造上平台的核心理由。
監造日報有全國公定格式（101.10.17 函），AI 監造報表未對齊公定列印。

**機關（最薄，但第一個付費客戶高機率是機關 → 優先補齊）**：
現有風險稽核+唯讀。缺：
- **跨案總覽**（承辦工程司同時管多案；風險稽核頁自己寫著「多案時可在總覽比較」但總覽不存在）
- **驗收/結算模組**（竣工申報→初驗→缺失改善→複驗→正驗→驗收合格→保固啟算），
  可串已有的契約義務解析

**跨角色共通**：
- **圖說管理無正式模組**（MarkupEditor 能標註但無圖說版次管理；現場拿錯版圖是真痛點；
  手上有 668MB 真實發包圖說可當測試資料）
- **My Open Items 跨案彙總**（現在「待你送出/改善」只在單案 Dashboard 內）

---

## 六、行動決議（2026-07-09，用戶拍板）

| # | 事項 | 決議 |
|---|---|---|
| 1 | 伺服器端 RBAC | **做** |
| 2 | 網域購買（pmis.ai）| **擱置**——專案名稱未定，不先花錢 |
| 3 | UI 修正包（上述 5 點）| **做** |
| 4 | store.jsx 拆分 | **做** |
| 5 | 監造端 ITP 停留點 | **做** |
| 6 | 機關端功能 | **優先全部補齊**（第一個付費客戶高機率是機關）|
| 7 | 行銷官網換新截圖 | **做** |

執行順序：報告/記憶 → UI 修正 → RBAC → store 拆分 → 機關端 → 監造端 → 行銷官網。
（RBAC/拆分是地基先行；機關端按客戶優先排在監造端前。）
