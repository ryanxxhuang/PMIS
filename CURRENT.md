# 目前系統現況

> CURRENT｜2026-09-19｜P4d 撤銷／減量／補證／調整 UI 與待監造補證球權＋P4c 估驗頁接上監造確認量後端強制＋P4b 監造確認量表／guard／RPC 已套正式＋P6c `audit.summary` 退場已套正式＋P3d 施工日誌列印印簽署版本與提送回執／退回歷史呈現＋P3b 自主檢查表＋P5d 履約時程 UI 與逐工項排程頁退場＋三方操作流程優化＋D-026 P1b 退場頁唯讀化＋P4a 純計算層＋P2a 現場文書資料層＋P2b Edge 起稿＋P2d 施工日誌存版／簽署／提送 RPC＋P5a 球權單一實作與待補設定＋P3a 監造日誌後端與頁面；正式發布狀態見 §6.3。
> 包含契約整理、Apple UI、D-022、工安清單／詳情殼，以及全案程式與文件整理；驗證見 [BASELINE](docs/BASELINE.md)。前端已由 main 自動部署；DB 已同步至 `20260919021500`（P5c 基準日版本；此前 H2／H3 `20260919003000`）、Edge 最後重佈 2026-09-19（P5c 重佈 `agent-run` v18／`send-reminders` v20／`fetch-weather` v17／`draft-field-documents` v5；此前 P5b 同四支、P2b 全部 18 支、P5a `agent-run`／`send-reminders`、P3a／P2c `draft-field-documents`），版本與核對範圍見 §6.3。
> 進行中工作（D-026 產品瘦身與四類文書、監造確認量估驗聯動）見 [續接清單](docs/reviews/2026-09-17-product-slimming-worklog.md)；本檔只記已上線現況：該包目前上線的是 P1a 導覽重組、P1b 退場頁唯讀化（成本寫入由 DB 收回、跨案選案清單、稽核檢核移入估驗頁、今日工作單一頁名；前端＋migration，demo 站已同步重佈）、P1c／P1d 文案對齊與抽屜斷點修正（前端）、P4a 純函式 migration（尚無呼叫端）、P2a 現場文書資料層 migration（表、RLS、guard、稽核）、P2b Edge `draft-field-documents`（照片批次辨識、候選文書、施工日誌 AI 版本；尚無前端呼叫端，P2c 才接）、P2d 存版／簽署／提送 RPC 與 `daily_logs` guard migration（只有 `daily_log` 簽署分支；尚無 Edge／前端呼叫端）、P5a 球權單一實作（首頁／Agent／早報同一份判定與共用案例、待補設定、`obligation_party()` 三方以外回 null；前端＋migration＋Edge 兩支）與 P3a 監造日誌後端（`supervisor_logs` 事實表、示範範本、到場只能人填、通用事實表 guard、`sign_field_document` 的 `supervisor_log` 分支、Edge 監造日誌起稿；migration＋Edge 一支）與 P3a 監造日誌頁面（`/supervisor-log`：監造審核／到場親自確認／簽署／提送機關、機關收件／退回、廠商唯讀、列印簽署版本；純前端），其餘仍是設計文件。

## 1. 產品與範圍

GovAgent 讓政府承辦人使用懂業務的 AI Agent；PMIS 是目前公共工程垂直領域的 repo。現階段只做公共工程，不預建其他領域框架。App 在 `app.gov-agent.ai`；`gov-agent.ai` 是另一個 `PMIS.marketing` repo 的行銷站；線上 demo 站 `demo.gov-agent.ai`（備援 `pmis-demo.ryanxhuang1212.workers.dev`；Supabase 留空建置，三角色選人登入、記憶體種子，需手動重佈）。

## 2. 使用者與授權

專案業務角色只有廠商 `contractor`、監造 `supervisor`、機關 `owner`。現場、品管、工安是廠商內部分工。平台管理員負責營運，不是第四個專案業務角色。

`profiles.org_type` 管三方業務身分；`project_members` 管專案存取與 admin；`project_parties`／`project_memberships` 管契約方身分快照。`project_role` 不作授權。D-022 已將專案管理統一由 `project_members.role='admin'` 決定，`created_by` 僅作稽核／建案用途；migration 已於 2026-09-11 套用正式庫。

## 3. 技術層次

React 18、Vite 6、Tailwind 4 SPA；Supabase Postgres／Auth／RLS／Storage／Deno Edge Functions；Cloudflare Workers 靜態資產部署；Sentry 錯誤回報。平台層處理身分、文件、期限、佐證、AI 閘門與稽核；公共工程層處理標單、日誌、品質、估驗與驗收。

## 4. 資料來源

- 工程數量／財務：`work_items` → 日誌數量 → 估驗／請款，品質與佐證以 `work_item_id` 串接。DB 結構以 migrations 為準。
- 文件／履約：`contract_packages` → `documents`／versions／pages → extraction runs → `requirements`／sources → `contract_obligations`。
- 只有 `approved` Requirement 是契約要求權威。所有已確認類型單向物化 obligation，執行狀態、佐證、罰則不反向改寫契約內容；被取代項只將仍待辦的 runtime 標不適用，保留歷史。

## 5. AI 邊界

AI 查詢、彙整、擬稿；業務核定、判定、結案、驗收與凍結由人執行。數字由確定性引擎計算。草稿／動作寫入 `agent_actions` 並由人接受或拒絕。

D-019 的契約轉錄例外：AI-origin 整理全部自動確認，即使有核對疑慮也進履約 runtime；核對是透明度註記，必須揭露原文優先。人工補登仍由監造／機關確認。AI 閘門失敗拒絕服務，用量記帳失敗不阻擋回應。

## 6. 技術與交付狀態

### 6.1 前端

- UI 採 [Apple 規範](docs/UIUX-Apple-設計規範.md)，token 在 `src/index.css`，圖示 `lucide-react`。`listDetail.jsx` 共用清單／詳情殼已供契約重點、擷取審核、工安、疑義、期限、送審、變更設計、停留點、提醒中心、三方成員、活動紀錄、風險稽核、品質三段、專案文件與缺失追蹤（`DefectTracker` 內建殼，/quality 與 /safety 共用）共十五頁使用；手機形狀依規範 §9（iOS 字級一處覆寫、44px 頂欄＋五格底欄、詳情推入、表格頁唯讀摘要、收件匣直達那一筆）。列印用 `.paper` 與 `PrintToolbar`。
- `navConfig.js` 是路由／導覽單一真相，未登記拒絕。側欄依 D-026 四主入口分區（2026-09-17 瘦身 P1a）：今日工作（球權三來源，持有 `/dashboard`）→ 工作（現場紀錄 `/site`／履約時程 `/requirements`／估驗請款 `/valuation` 三組＋子頁，子頁依角色過濾；期限追蹤與擷取審核改為履約時程子頁）→ 專案資料（文件往來：送審／疑義／施工月報／監造月報；專案：專案文件／三方成員／活動紀錄／跨案總覽）→ 平台。`/cost`、`/audit` 為 hidden（不進側欄／分頁列／尋找功能，仍登記且依原 roles 可深連結）且自 2026-09-17 P1b 起唯讀（見下一點）；`/schedule` 自 2026-09-19 P5d 起亦 hidden 且唯讀（關鍵工項的計畫起迄與落後判定承接到 `/requirements` 履約時程的同一條時間軸，廠商在事項詳情維護；排程頁只剩歷史查閱與 CSV、每列「到履約時程」帶 `?item=` 直達）；`roles` 未變。新頁 `/site`（現場紀錄總覽）依角色列出日誌／查驗／自檢／試驗／停留點／工安入口與件數、現場待辦（與今日工作同一份聚合）與本月文件入口；2026-09-17 P2c 起加入「拍照／上傳」主動作、「上傳批次」（從伺服器恢復）與「現場文書」清單（見下一點）。群組與底欄短標 `short` 與 label 同住 navConfig；待辦「返回來源」（今日工作／提醒中心／現場紀錄）統一在 `src/lib/taskReturn.js`，名字取自 navConfig（`BALL_SOURCES_TITLE`、`/alerts` 的 `label`、`/site` 群組）；`/dashboard` 的 h1 也是 `BALL_SOURCES_TITLE`，全站只有「今日工作」一個名字（2026-09-17 P1b 統一，舊「今日待辦」別名移除）。
- 退場頁唯讀化（2026-09-17 瘦身 P1b，D-026 §4／§5）：**成本管理 `/cost`** 只剩歷史查閱與 CSV 匯出——寫入由資料庫收回（migration `20260917210000_cost_items_retire`：收回 authenticated／anon 的 INSERT／UPDATE／DELETE grant、policy 改 select-only；讀取條件不變，監造／機關仍讀不到廠商成本），store 不再有成本寫入函式，頁面無新增／編輯／刪除控制。**跨案總覽 `/portfolio`** 縮為選案清單（案名／代碼／狀態、未結缺失／待查驗／待核定變更件數、最近估驗期，整列可點切換），統計、進度、驗收階段與例外彙總帶及 `portfolioExceptions.js` 移除；`portfolio_summary` RPC 未改。**風險稽核 `/audit`** 保持 hidden＋僅機關，頁首標「唯讀查閱」並導向估驗計價；估驗所需的勾稽檢核改在 **估驗計價頁** 逐期顯示「本期勾稽檢核」卡（`src/lib/valuationChecks.js` 組裝＋既有 `integrityAudit.js` 引擎，稽核頁共用同一支），Agent 稽核提示卡的連結改指 `/valuation`、三方成員頁的機關權限說明不再列風險稽核；契約／變更／進度檢核表面向暫留 `/audit` 唯讀（P5d／P6b）；AI 稽核意見 `audit.summary` 已於 2026-09-19 P6c 退場（migration `20260919130400` 關閉平台總開關、伺服器閘門對舊呼叫回 403，前端按鈕／store 路徑移除，用量歷史與 `audit_events` 不動；正式套用狀態見 §6.3）；決策列「超計／無佐證」與整期勾稽發現的口徑合併與缺件控制留 P4c。
- 首頁為「現在輪到我／等待對方／今天已完成」單桶收件匣，`?ball=` 選桶；可搜尋、依類型／逾期與今天到期篩選、每次 20 件原地顯示更多。篩選及載入筆數保存在 URL，待辦進入單據後可返回原清單。手機篩選預設收合。待辦共用 `todayTasks.js`／`useTodayTasks`；今天已完成明示僅含有可靠時間戳的缺失與查驗。
- 三方主入口由 `navConfig.MAIN_ENTRY_PATHS`／`roleWorkLinks` 產生，三角色相同＝三個主入口群組（現場紀錄／履約時程／估驗請款），在任一子頁底欄群組仍為選取；桌機首頁的主入口列名稱＝側欄「工作」分區名（`WORK_TITLE`，2026-09-17 P1c 取代「廠商常用」），手機五格底欄＝現在輪到我＋三主入口＋更多。`ROLE_WORK` 只剩角色標籤與摘要。頁面文案指路的頁名取自 `navLabel`／`navEntryFor`（提醒中心指向「履約時程」、初始化清單指向「三方成員」、建案表單與期限追蹤指向「契約重點」），不手抄字串。提醒中心明示與「今日工作」同一份事項、只是依期限分類。側欄「尋找功能」依既有權限搜尋頁面名稱與操作提示，不呼叫 AI。各工作頁頁首說明依角色呈現。
- 手機導覽抽屜（2026-09-17 P1c 缺陷修正）：抽屜只存在於 `<md`，`Layout` 以 `drawerOpen = isBelowMd && menuOpen` 推導，鎖捲動／Esc／遮罩／焦點／`aria-expanded` 全讀它；抽屜開著拉寬到 ≥768 時鎖定隨抽屜消失而解除、捲動位置還原、開啟意圖清除（`a11y.spec.js` 釘住）。
- 查驗／觀察／試體／停留點待辦直達詳情；估驗與付款 `?period=` 指定期別，不存在的期別提供重新選擇入口。共用詳情 sticky 避開頂欄，對話框支援焦點圈限與返回觸發點。
- 入口與待辦（2026-09-15 UIUX 階段 2，部分）：真專案未匯標單時，首頁「現在輪到我」同時顯示專案初始化卡與已可執行的待辦（送審／疑義／履約期限不依賴標單），非專案管理者看到「由專案管理者完成」說明；風險警示與 AI 代辦仍只在有標單時顯示。提醒中心的關鍵字／期限類別篩選保存在 URL，「前往處理」返回時篩選不遺失。桌機 AI 入口：頂欄輸入框可及性名稱改為「問 GovAgent」，側欄「問 GovAgent」由浮起大按鈕降為一般導覽列，Copilot 維持薄入口；「尋找功能」仍為不依賴 AI 的頁面查找。
- 入口方案 B（2026-09-15 採用，階段 2a）：進頁或切換路由時，側欄自動展開目前頁所屬的工作群組並列出同組子頁；使用者仍可手動收合／展開，狀態只保留在本次瀏覽。頁內分頁列 `PageTabs` 只在側欄提供不了同組子頁時渲染（側欄收合成 icon rail、平板 768–1279、手機抽屜、或使用者手動收合該組），由 `SidebarNavContext`（`src/lib/sidebarNav.js`）傳遞；不在 `WebLayout` 底下時一律渲染。路由登記與 roles 未變。
- 工作交接與回找補強包 B（2026-09-16，依 Codex 實測報告 W05／W06／W08／D01）：品質頁切分段與點佇列、共用 `useUrlFilters`、日誌切日期改寫 URL 時都保留 `location.state`，從待辦進來的「返回今日工作」不再消失。返回時除原項 key 另帶 `returnedTo`（剛才那一筆的頁面與選取）；首頁原項已離開清單時不再指向不一定找得到它的「今天已完成」，改給「回到剛才處理的那一筆」連結。送審建立後的提示與文件區只給當下做得到的指示：附件說明只在建立時填（表單加提示，建立後不可修改），外部交件未註明可於補正再送時寫在補正說明；未放寬已提送件的修改權限。示範模式（非真專案）不再顯示會失敗的上傳鈕，改為說明正式專案才可上傳。
- 報表口徑與資訊層級補強包 C（2026-09-16，依 Codex 實測報告 W07／W09）：W07 只做口徑對照與最小修正提案（`docs/reviews/2026-09-16-uiux-w07-progress-caliber.md`），未改任何進度公式；查明施工月報與監造報表的預定值差異來自預定進度表「月份列」被兩處分別解讀為月底與月 1 日（C1），另列未核定估驗一律計入（C3）、月報收款欄不隨月份（C4）等待決。W09 進度頁排行更名「估驗完成率差距（依金額權重）」並說明不是工項排程落後判定，排序公式不變。
- 進度口徑定案 D-024（2026-09-16，補強包 C 第二批）：預定進度表月份列＝月底累計，`plannedPctNow` 改在上一列與本列之間按日內插（今日預定不再高估一個月）；新增 `src/lib/progressAsOf.js`（`reportCutoff`、`latestValuationAt`），施工月報與監造報表同一報告月份取同一截止日（過去月份月底、本月今天）與同一期估驗，八處進度數字共用取期規則（估驗日期不在未來、狀態不論）；月報收款／請款期數截至截止日；三頁都標示截止日、所取期別與是否含未核定。Demo 種子估驗比例下修、第 5 期日期改為 3 天前以保留落後示範。
- 版面與操作感受小包 D（2026-09-16，依 Codex 實測報告 §5 五項）：變更設計詳情去掉與狀態列／決策摘要重複的四格（只留提出日）；送審詳情四格去掉類別與狀態（徽章與球權章已帶）、AI 助手區預設收合（有結果或執行中自動展開），1440×900 下「受理審核」從首屏外進到首屏；送審審定對話框標題與按鈕改用動作名（受理審核）而非狀態值（審核中）；標單工項頁新增「搜尋工項」（項次或名稱，關鍵字存 URL `?q=`，只列符合列與所屬各層並標記符合列、找不到明說、清除回原展開；手機章節摘要不套用）；施工日誌未存檔時明說「先存檔即可直接上傳照片（不辨識）」；品質／工安頁底部「狀態機」改為「改善順序」。不刪任何未儲存、未附檔、AI 暫存或責任方資訊。
- 輸入與資訊可信度補強包 A（2026-09-16，依 Codex 實測報告 W01–W04）：`unsavedEdits` 新增站內離頁保護——有未存檔登記（施工日誌、自主檢查表）時，點任何站內連結先確認，取消留在原頁，確認後清登記並放行原連結（保留 Link state）；同路徑只改 query、外部連結、修飾鍵點擊不攔；瀏覽器返回鍵與程式 `navigate()` 不經此保護，日誌「列印公定格式日誌」自行確認。送審詳情的監造摘要：再送後尚未受理（已提送）才標「上次退回原因」，受理／審定後改標「最新審查意見」並註明歷次退回原因未另行留存（`review_note` 只有最新一則）。自主檢查表在編輯判定旁、存檔訊息、紀錄列、查驗申請檢附選項與查驗詳情都顯示覆蓋程度「已檢 n／總數，m 項未檢」（`qc.js` 的 `checklistCoverage`，不改 `overall` 與提送資格）。監造報表意見草稿改為只寫資料能支持的數量與現況，到場、促請、品質符合等判斷改為「請補充」由人填；Demo 估驗佐證包草稿依 `photo_count` 說明有無照片，零張時寫「尚未檢附現場照片」（正式 Edge 生成未改）。
- 跨角色收尾（2026-09-15 UIUX 階段 6）：送審、疑義、變更設計、提醒中心的關鍵字與分段篩選都存 URL（共用 `src/lib/useUrlFilters.js`），只放可分享的識別；日誌待辦直達 `/site-log?d=`，切日期同步 URL；清單／詳情殼的深連結指到不存在的單據時回報 `missingId`、移除失效參數並改選預設，四頁顯示說明；從單據返回今日待辦而原項已離開清單時顯示說明。機關首頁角色摘要改為「登錄付款紀錄」。驗收矩陣、文案核對與真人測試腳本見 `docs/reviews/2026-09-15-uiux-stage6-acceptance.md`；真人驗收與正式後端未測。
- 驗收當前階段與下一步（2026-09-15 UIUX 階段 5C）：驗收待辦深連結改帶 `?stage=<階段鍵>`；驗收頁新增「目前階段」卡（當前關、法定期限、主辦、可登錄者、前置階段完成日、依據、依角色的下一步與「前往登錄」聚焦），時間軸當前列標 `aria-current="step"` 並在深連結指到當前階段時捲到該列。深連結指到已完成、尚未輪到或不存在的階段只顯示說明（含目前階段），不在別的階段開編輯。登錄前顯示核對句「將登錄：階段 · 日期 · 結果」（結果未選明寫尚未選，不預選合格）；登錄成功後顯示「已登錄 … 下一階段：…（責任方，期限）」。期限演算法、階段順序、可登錄角色與改善／複驗推導未改。
- 機關變更核定的資訊順序（2026-09-15 UIUX 階段 5B）：變更設計詳情依「目前責任與事由 → 已知金額影響 → 明細（相對原契約）→ 本系統未登錄 → 核定動作」排列。摘要區依狀態與角色標「輪到你受理審查／待監造受理審查／待你核定／待機關核定／已核准／已駁回」，列事由、理由（未填寫則明說）、淨額與明細筆數，待核定件由 store 的變更後契約金額加淨額確定推導「核准後變更後契約金額將為」；工期影響、監造審查意見、附件三項固定明寫未登錄／未提供／無（資料模型無此欄位，不由 AI 補）。機關核准／駁回各加共用確認框，內容指向單據編號、淨額與核准後金額，取消不寫入；受理／退回（監造）、編輯與刪除（廠商）、核定順序、金額公式、凍結與撤銷限制未改。清單頁的全案四張金額卡移到清單與詳情之後，標「全案金額（只計已核准變更）」。
- 請款收款逐欄保存回饋（2026-09-15 UIUX 階段 5A）：沿用逐欄 `onBlur` 寫入與既有前置條件（未核定鎖定、收款日需先有請款日、實收需先有收款日）。頁面明示「離開欄位後即儲存該欄，三欄各自寫入」；每次寫入在欄位下方顯示儲存中／已儲存／未儲存（失敗時輸入值留在框內、註明正式值仍是什麼、可「還原」或修正後再離開欄位重試）；日期驗證失敗不打 API、同樣標未儲存；溢收確認取消時輸入框拉回正式值並說「已取消，未儲存」；缺前置日期就近提示。指定期別進入時，表只列該期並以「第 n 期請款／收款登錄」為題，統計卡另標「全案累計（已核定期別，共 n 期）」，匯出鈕標「全案 n 期」；不存在的期別不列任何列並說明未變更紀錄。頁首明示此頁只登錄紀錄、系統不執行付款。金額計算、角色權限、手機唯讀規則未改。
- 監造送審審查順序（2026-09-15 UIUX 階段 4）：送審詳情對可審定者依工作順序排列——狀態列 → 結果提示 → 「待受理／待審定 · Rev.n 首次提送／補正再送」摘要（為何輪到我、上次退回原因取 `review_note`、本次補正說明取 `attachment_note` 最後一行「補正(Rev.n):」，沒有就明寫）→ 編號與 MetaGrid → 文件與提送方式（含 AI 助手：兩項能力各附資料範圍與「結果只保留在本頁」，功能關閉或未上傳文件本體時如實說明，無 AI 仍可審定）→ AI 結果區 → 「審查意見與決定」（受理段：受理審核／退回補正；審定段：核准／核備／退回補正／駁回，附退回與駁回後果說明）。合法轉移、對話框與必填原因不變，頁面另加空白原因不寫入的檢查。決定成功後留下結果與下一責任方，「下一件待審（n）」由人點才換單；回原佇列由頁首「返回今日工作」與左欄清單承擔。疑義與查驗詳情未改，另列下一包。
- 現場文書第一條完整路徑（2026-09-17 瘦身 P2c，D-026；後端 P2a／P2b／P2d）：**施工日誌是「文件」**——`/site-log`（`?d=` 日期或 `?doc=` 文件）逐欄顯示值、來源（照片 AI 說明／告示板轉錄／沿用昨日／既有紀錄／氣象署／人工）與狀態（已帶入・待核對／待補／不適用／已確認），待補集中在頂部可點到該欄；存檔＝`save_field_document_version`（伺服器保存版本、算雜湊、判待補；沒有文件先 INSERT 草稿），版本落後（`PD001`／`PD002`）明確提示「重新載入最新版本」且輸入留著；簽署卡顯示版本、雜湊前 12 碼與意願聲明，人確認後 `sign_field_document`（以登入的平台帳號簽署，沒有驗證碼步驟；R1 2026-09-19），簽署成功事實表 `daily_logs`／`daily_log_items` 由 RPC 落庫並重載 `siteLogs`；已簽署／已提送要改要先按「建立更正版本」（舊簽署綁舊版）；提送給監造、監造收件／退回（必填原因）各走 RPC 且 `client_request_id` 存 sessionStorage 重試沿用；歷次提送／退回原因／再送 diff 與簽署紀錄列在文件卡；監造／機關唯讀（除日期外無 input）。**前端沒有其他寫 `daily_logs` 的路徑**：`saveSiteLog`／`deleteSiteLog`／逐張 `classifySitePhoto`／`readWhiteboard`（板上未寫數量填 0）與批次辨識覆核區都已移除；Agent 收件匣接受日誌草稿改為存成文件人工版本（`applyDailyLogDraft`），再到施工日誌頁簽署。既有未簽署日誌（正式 12 筆）開啟時以其內容帶入、來源標「既有紀錄、待核對」，第一次存檔才建文件；列印（P3d）：`/site-log/print?doc=` 印簽署列指向的版本（頁首短碼、版本、DB 雜湊前 12 碼、簽署者與伺服器時間），未簽署印最新存檔版本並整張標「草稿・未簽署」，只有舊流程既有列時如實標無版本與雜湊；文件卡另列「提送與回執」（對象、送出時間、完整回執編號、收件狀態、下一責任方）與「退回歷史」（歷次原因、退回人、時間、補正差異），四類文書共用；畫面上的伺服器時間一律台北時間。`/site`：「拍照／上傳」（`IntakeUploader`：選檔即建 `photo_intakes`、逐張算 sha256、本案已有同內容照片不重傳、同批同雜湊只傳一張、`photos` 列寫成功才標「已保存到伺服器」，其餘標「仍在本機」；至少一張已保存就呼叫 `draft-field-documents` 並續跑 `remaining`；結果列逐張狀態、候選文書（已就緒／待補／尚未支援／已排除，使用者可排除）與文件連結，`notes` 有 stub 時明標）、「上傳批次」（我建立、未捨棄的批次從伺服器恢復，下一步由狀態推得：開始／繼續／重試辨識、補日期、捨棄）、「現場文書」（與今日工作球權同一份 `fieldDocuments`，施工日誌可開頁，其餘三類——含 P3a 後端已起稿的監造日誌——頁面接上前只列狀態並標尚未支援）；P5a 待辦的 `/site?doc=` 落到 `/site` 後轉 `/site-log?doc=`。示範模式：草稿只進記憶體，上傳／簽署／提送明確回「示範模式無法…」，不假裝已簽署。本機 Edge 可用 `PMIS_VISION_STUB=1` 的確定性 stub（只在本機 http 位址生效，正式 Edge 永遠關閉），供真後端 E2E；模型品質未驗（P7b）。
- 監造日誌頁（2026-09-19 瘦身 P3a 前端，D-026 四類文書之二；後端 P3a）：**監造日誌也是「文件」**——`/supervisor-log`（`?d=` 日期或 `?doc=` 文件；「現場紀錄」群組子頁，三角色皆可進：監造可編／簽／送，機關為提送對象可收件／退回，廠商可讀但唯讀）。欄位版面由伺服器範本 `fn_field_document_template('supervisor_log')`（示範範本）的八節驅動，頁面與列印都標「示範範本」與免責聲明，必填鍵、人填欄、欄位名由範本推導（示範模式讀 `src/data/demoFieldDocTemplates.js` fixture，測試釘住與 Edge 鏡像一致）。逐欄顯示值、來源（照片 AI 說明／查驗紀錄／系統缺失／查驗／文件狀態／同日施工日誌文件／氣象署／人工）與狀態；監造事項逐項標來源、照片張數與工項；當日查驗以系統紀錄列出可勾選；通知／追蹤的引用以缺失／查驗名稱顯示；來源缺漏一律留待補，不填「無」。**到場人員只能人填**：AI 草稿永遠留空；帶入本人／本案監造成員或手填後只是「已填・待親自確認」，明確按「確認到場人員」才 `confirmed`，確認後再改回到待確認，「本日未到場」＝不適用＋原因——與伺服器 `needs_confirmation` 同一條規則（頁面預覽＋簽署 `PD004` 高亮）。廠商施工情形只在同日施工日誌已簽署／提送／收件時引用（`field_document:<id>:v<n>`，「引用同日施工日誌」與 Edge 起稿走同一支 `_shared/fieldDocText.ts`），草稿／退回的只在收件情形揭露現況。附件：廠商照片標「施工廠商提供」、只能以「參考」附上（伺服器 `PD005` 是邊界）。存檔＝`save_field_document_version`（版本落後 `PD001`／`PD002` 提示重新載入且輸入留著）、簽署＝`sign_field_document`（簽署以登入的平台帳號完成、沒有驗證碼步驟（R1）；事實表 `supervisor_logs` 由 RPC 落庫）、提送給機關／機關收件／退回同施工日誌的 RPC 與回執；簽後更正要先「建立更正版本」。`/supervisor-log/print?doc=` 印簽署列指向的版本（短碼、版本、雜湊前 12 碼、簽署者與伺服器時間），未簽署整張標「草稿・未簽署」。`/site`：監造的拍照／上傳標「監造日誌自動起稿」，現場文書清單依文書類型判「輪到我」並直達頁面（自檢表／查驗表單仍標尚未支援），「現場作業」多一列監造日誌入口。共用元件只擴充不複製（`DocumentLifecycle` 依 `doc_type` 決定責任方／提送對象、`DocumentPhotos` `ownerOrg`、`FieldSourceChip` `humanOnly`、`RowsEditor`／`WeatherPull` 自施工日誌抽出），施工日誌行為不變。示範模式：草稿只進記憶體，簽署／提送明確回「示範模式無法…」。模型品質未驗（P7b）。
- 廠商每日填報的資訊層級（2026-09-15 UIUX 階段 3C）：施工日誌可編視角的區塊順序改為「日期＋保存狀態章（本日尚無日誌／未存檔／存檔中／已存檔 HH:MM，時間為本次存檔時刻）＋帶入天氣／複製昨日 → 天氣與摘要 → AI 草稿卡（仍標草稿、須人存檔）→ 本日施作數量 → 現場照片（併入本日日誌卡；唯讀視角維持獨立卡）→ 公定格式欄位（預設收合，摘要列顯示已填列數與『尚未填：出工人數／機具使用／材料使用』的展開入口）→ 貼底存檔列（桌機也 sticky）」。切日期有未存檔輸入先確認，取消留在原日期；未存檔輸入登記到 `unsavedEdits`（切案先問、重新整理由瀏覽器提示）。照片上傳／刪除的結果只顯示在照片區（成功幾張、第幾張失敗），日誌存檔結果只在存檔列。dirty 保護、複製昨日、照片草稿、列印、正式輸出欄位與估驗數量來源未改。
- 廠商品質流程與未存檔保護（2026-09-15 UIUX 階段 3B）：品質頁分段寫進 URL `?seg=`（inspections／defects／observations／checklist／samples），單條參數仍優先決定分段；自主檢查表分段常駐掛載、非當前只隱藏，切分段或點佇列不再卸載編輯中的實測值與更正原因，`key` 綁專案／身分，換案即重置。未存檔輸入（實測值、位置、更正原因、工項）登記到 `src/lib/unsavedEdits.js`（不存內容、不跨頁恢復）：分段 chip 標「未存檔」、表單內提示、取消先確認、重新整理／關閉由瀏覽器 `beforeunload` 提示、頂欄切換／新增專案先確認；側欄換頁與瀏覽器返回仍不攔（HashRouter 無 `useBlocker`）。自主檢查 → 申請查驗維持預填工項與現行版檢附；送出失敗表單與檢附留著；成功顯示「已送出、已檢附 … 、等待監造現場查驗」並選中新查驗（`createInspection` 回傳新 id）。自動判定、缺失建立、修訂證據凍結與權限未改。
- 廠商送審提送與補正（2026-09-15 UIUX 階段 3A）：建立後仍即為「已提送」，無草稿狀態。新建成功自動選中該筆並在詳情顯示「提送紀錄已建立」與附件狀態；詳情的「文件與提送方式」區同時放附件說明、由 `attachment_note` 的「補正(Rev.n):」行拆出的補正紀錄、文件本體與上傳／更換、外部提送說明；表單明示建立紀錄與上傳文件是兩個步驟。退回補正件（廠商）在詳情頂端先看退回原因、目前版次與附件狀態，「修正再送」就在該區塊內（動作列不再重複）。上傳失敗只顯示失敗且附件狀態不變、可重選；上傳成功與再送成功各自只描述完成的部分（再送顯示新版次與等待監造受理）。結果提示只存在本頁 state；`review_note` 只有最新一次退回原因，歷次原因不可分列。
- 操作可信度（2026-09-15 UIUX 階段 1）：驗收需結果的階段必須明選合格／不合格，未選不發寫入並就近提示；新送審／新疑義建立失敗時表單與輸入（含圖面標註）保留、錯誤可重試、送出中不可連點，成功才收表單並選中新紀錄（`createSubmittal`／`createRfi` 回傳新 id）；送審 AI 助手文案明示結果只保留在本頁，離頁需重新執行（無背景任務或結果持久化）；驗收頁首保留專案名稱，疑義與付款的角色提示只描述既有動作。
- 預定進度在首頁、進度頁、稽核、監造報告、跨案總覽與 AI 資料共用 `src/lib/progressPlan.js`。沿用月份座標、30 天換算與線性內插；不變更 S 曲線產生或 DB 計算。
- 公開頁 `/security`、`/terms`、`/privacy`（條款／隱私為 0.9 草稿，待律師審閱）。兩步驟驗證已於 2026-09-19 整個移除（R1，使用者決定）：登入只有帳密一步、`/account` 頁與頁首「帳號」連結不存在、簽署不要求 aal2；正式 Supabase Auth 的 TOTP 應在 Dashboard 關閉（待使用者執行，步驟見 `docs/operations/deploy.md` §7）。

### 6.2 驗證

當前結果與指令只見 [BASELINE](docs/BASELINE.md)。單元、Demo E2E、本機真後端 E2E 固定資料模式、pgTAP、18 支 Edge 的 Deno 型別檢查均通過；2026-09-17 P4a 新增一支只含純函式的 migration `20260917120000`、P2a 新增資料層 migration `20260917201000`、P2d 新增 RPC 與 guard migration `20260917205000`（皆已套正式，見 §6.3；pgTAP 從零套用 43 檔 1,486 通過）。真模型抽取仍未驗，抽取準確率／召回率未用真契約量測；`completed` 不代表語意正確。真人手機輪與真案三角色實機驗收仍待完成。

### 6.3 正式環境最後核對（不是即時狀態）

- **2026-09-19 P4d 撤銷／減量／補證／調整（D-026 第 3 點；實作指令 §3.3；設計文件 §18）**：PR #147（分支 `codex/slimming-p4d-adjustments`，commit `a9bff38`＋docs 同步；rebase 到含 T1 的 main `89fbd2c`；merge commit 由下一單元補）。migration `20260919160000_vq_message_numeric_format`（新增純函式 `fn_cq_txt`＋`create or replace set_valuation_item_cum`，只改 `VQ005`／`VQ006` 訊息的數字格式 `60.0000`→`60`；不動表、policy、資料；回復檔 `supabase/rollbacks/` 同名）——**正式套用結果由下一單元或使用者回填**。Edge 只改共用模組 `_shared/ballInCourt.ts`／`ballInCourtRules.ts`（早報與 Agent 的「待監造補證」「待機關處理扣回」要等下次 `send-reminders`／`agent` 部署才生效，尚未部署）。內容：估驗頁來源展開多「有效確認 n 筆」，監造逐筆撤銷（原因必填，DB 回傳的收斂結果翻成人話）、監造確認單減量、簽發監造確認單（就地表單、冪等 `client_request_id`）、已核定期有歷史遷移來源時「補證此期」（`issue_supervisor_certificate` 帶 `p_covers_valuation_id`，介面明講成為該期計價依據與涵蓋範圍、累計量不得小於遷移量）；「估驗調整（扣回）」卡三方同一張，機關作廢待處理扣回（明講接受已計價、不產生新可用量），廠商／監造唯讀；缺件卡 `certificate`／`adjust`／`review` 指到這些入口；今日工作／Agent／早報共用規則：已核定且未補證 → 「待監造補證」、pending 扣回 → 「待機關處理扣回」（新事項類型「估驗調整」）。**正式庫 4 個歷史遷移期別的補證方式**：監造登入→估驗頁選該期（首頁「待監造補證」可直達）→缺件卡「展開 1 列」→來源展開按「補證此期」→填批次／位置與依據（累計量預填遷移量）→「簽發並補證第 N 期」；缺件清空後機關在 `/payments` 登錄請款日。demo 站不重佈；正式站登入後真人驗收未核對。
- **2026-09-19 P4c 估驗頁接上 P4b 後端強制（D-026 第 3 點；實作指令 §3.3；設計文件 §17）**：PR #144（分支 `codex/slimming-p4c-valuation-ui`，commit `88cdcfe`＋docs 同步；rebase 到含 P3b 的 main `066f247`）。純前端、無 migration、無 Edge；merge commit `8acb9b8`，Workers Builds 隨 main 建置（主 session 回報 success），P4d 於同日 `check:prod` 五頁 OK。內容：估驗頁只顯示 DB 結果、只經 P4b RPC 寫入——可估驗清單（`list_billable_backlog`；未開期先累積）、期別狀態（`get_valuation_state`）、建期必填計價截止日（Q7，對話框預填台北今天，建期後由 DB 同步可估驗量）、改累計量走 `set_valuation_item_cum`（超上限 `VQ006` 顯示前期累計／本期起算值／上限／可用量並把輸入框回到 DB 值）、送審／退回／核定走 `transition_valuation`（帶目前狀態，`VQ004` 逐項翻成人話列出原因）、動作列一顆「同步確認量」（冪等）、監造在明細列設定總價／間接費計價依據（Q3 缺依據顯示「計價依據待設定」且不計價）；每工項來源展開列批次／位置／確認累計／查驗與文件版本／確認人與時間，legacy 來源標「歷史遷移，非監造確認，需補證」；依據標示「缺監造確認來源・申報,不計價」／「監造確認」／「管理員調整」；核定前期別的可請款金額排除有缺件工項的本期增量（回到前期累計）；「缺件與檢核」卡合併 DB 檢查點缺件（送審前可見、給處理入口）與六項勾稽發現為單一口徑（`lib/valuationChecks.js`；`valuationDiff.js` 刪除），日誌申報量只作差異比對（「超前日誌申報／無日誌申報」）；金額改讀 DB `amount_cum`（`buildCumMap` 不再換算，十個頁面／lib 呼叫端同口徑；期別投影往前帶 `lib/valuationPeriods.js`）；`fillValuationFromSiteLogs` 與所有客戶端寫 `valuation_items` 的路徑移除（P4e 收回前，舊客戶端 REST 寫入在頁面呈現為申報未確認不計價，真後端 chain 9 驗證）。撤銷／補證／作廢 UI 見 P4d。demo 站不重佈；正式站登入後真人驗收未核對。
- **2026-09-19 P4b 監造確認量的表、guard、RPC、鎖（D-026 第 3 點；設計文件 §16）**：PR #138 已由使用者合併（merge commit `69a1e30`，分支 `codex/slimming-p4b-confirmed-qty`，commit `bb17579`＋`79460c9`＋`6ef8adb`）。使用者於 2026-09-19 親自 `supabase db push` 套用 `20260919140000_confirmed_quantity_enforcement`（套用前 `migration list --linked` 只差這一支，套用後 74 筆對齊；輸出「Finished supabase db push.」）。使用者以 `supabase db query --linked` 唯讀核對，實際值全部與 P4b 預期一致：`inspection_confirmations` 0、`valuation_item_sources` 5（全部 `kind='legacy'`，即 4 個已核定期的歷史遷移回填）、`valuation_adjustments` 0、`work_item_pricing_basis` 0、`valuations` 12、`valuation_items` 26（`backing='legacy'` 26）、`recheck_required` 0、含 legacy 來源的已核定期 4、新增 trigger 11、`fn_cq_*` 函式 29、authenticated 可執行的 RPC 10（authenticated 函式 EXECUTE 總數 79、anon 0）、四張新表對 API 角色無多餘表級 grant（0）、`valuation_adjustments.applied_valuation_id` FK＝`ON DELETE SET NULL`。無 Edge；前端當時無改動（估驗頁由 P4c 接上）。正式庫既有 4 個已核定期自此起「登錄請款日／→已請款」會被檢查點擋下並列出 `legacy_source`，補證路徑為監造確認單 `issue_supervisor_certificate(p_covers_valuation_id)`（UI 在 P4d）。
- **2026-09-19 P6c `audit.summary` 退場（D-026 §4）**：PR #141 已合併（merge commit `b0571bf`，分支 `codex/slimming-p6c-retire-audit-summary`，commit `b2c3aa4`＋`ca30702`）。2026-09-19 13:30Z 使用者 `supabase db push` 套用 `20260919130400_audit_summary_retire`，`migration list --linked` 73 筆對齊；正式庫唯讀核對：`ai_features.audit.summary` `enabled=false`、該功能用量 0、`ai_features` 18 列其中關閉 3（三支退場功能）、`audit_events` 1,297 筆不動。無 Edge 重佈（閘門讀 DB 即時回 403）；前端隨 main 由 Workers Builds 建置 success；`npm run check:prod` 五頁 OK；demo 站未重佈。
- **2026-09-19 P3d 施工日誌列印與提送回執／退回歷史呈現（PR #145，純前端）**：合併前本機驗證見 [BASELINE](docs/BASELINE.md)；無 migration、無 Edge、無新路由（沿用已登記的 `/site-log/print`）。前端隨 main 由 Workers Builds 自動建置，合併後 `check:prod` 結果寫在單元回報、由下一單元同步；demo 站未重佈。上線內容：施工日誌列印印簽署版本與雜湊（未簽署標草稿・未簽署）；三個列印頁共用選版本與頁首戳記；文件卡「提送與回執」「退回歷史」（四類共用）；現場文書的伺服器時間改台北時間（原以 UTC 顯示差 8 小時）。
- **2026-09-19 P3b 自主檢查表（PR #143，migration `20260919141500_self_check_documents`）**：合併前本機驗證見 [BASELINE](docs/BASELINE.md)；合併後的正式套用（`supabase db push`）、`draft-field-documents` 重佈與 `check:prod` 結果寫在單元回報，由下一個單元同步到這裡（使用者 2026-09-19 要求合併後不另開文件 PR）。上線內容：`fn_field_document_template('self_check')` 示範框架範本（介面與列印標「示範範本」）；自檢表必填／人填／須確認欄由框架＋本案 `checklist_templates` 項目推導；`sign_field_document` 的 `self_check` 分支寫 `checklist_records`（首簽 Rev.0、簽後更正重簽＝修訂 Rev.N，更正原因取版本變更說明）；判定引擎 `fn_checklist_judge` 與不合格開缺失 trigger 下沉 DB（既有直接登錄路徑照舊可用，判定改由伺服器重算、缺失改由 trigger 開）；Edge `draft-field-documents` 起自檢表草稿（每項待補、實測值不帶值）並改為執行期向 DB 取範本；前端 `/self-check`、`/self-check/print`、`/site` 入口、查驗申請檢附已簽署版本。
- **2026-09-19 P5d 履約時程 UI 完整化與逐工項排程頁退場（D-026 §4「必要關鍵工項日期承接到履約時程後，再退場逐工項排程頁」；實作指令 §5）**：PR #140 已合併（merge commit `0cca888`，分支 `codex/slimming-p5d-schedule`，commit `e69b8b3`＋`093f652`，rebase 到含 R1／CI1 的 main `c221c67`）。PR 檢查 CI／pgTAP 於 `093f652` 皆 success。純前端、無 migration、無 Edge。Workers Builds 對 `0cca888` 建置 success（GitHub check-run「Workers Builds: pmis」completed，非 D1 那次的 queued 空等）；`npm run check:prod` 五頁（app `/`、`/login`、`/demo/`；demo `/`、`/login`）全 OK；正式入口 chunk `index-CGhdBv5p.js` 已含 P5d 的 `/requirements` 指引文案，`Requirements-CpOj92g6.js` 含「時程範圍」「加入關鍵工項」「退回 … 期待辦」、`Schedule-BefRMlMA.js` 含「已退出新作業」「到履約時程」；demo 站未重佈。內容：`/requirements` 履約時程把關鍵工項（`item_schedules` 的計畫起迄＋最新一期估驗累計 ÷ 契約數量的完成%，落後判定與退場前排程頁同一份 `lib/keyWorkItems.js`）與停留點（`inspection_points` 由查驗推導，施作中未叫驗 H 紅／W 黃與 `/itp` 同一條規則）以事項 `wi:<key>`／`itp:<id>` 併入契約義務的同一條時間軸（同一份快篩／類型／搜尋／詳情；指定契約範圍時只列該契約義務）；近期（逾期最久在前 → 7 日內 → 待補設定／無到期 → 30 日內排程 → 最近 7 日完成，每列標期程；空時自動退回全期）／全期（五段期程分組）Segmented；「待補設定」下拉列責任方／基準日／循環規則／停止條件／回填待核對五種缺口件數可篩（判定沿用共用規則 `obligationEntries`，處理入口與今日工作同一張對照 `lib/obligationLinks.js`；基準日／停止條件就地打開履約期程卡的基準日編輯列，責任方／循環規則導擷取審核，回填待核對在期次區標記即解除）；循環義務逐期在本頁標記完成／退回待辦（掛佐證隨完成走 `transition_obligation_period`，不再導到期限追蹤），逐期準時率（`periodStat`：分母已完成＋已逾期的期）在期次區、執行卡準時率對循環義務以期計入（五狀態仍以條計）；佐證直達 `/submittals?submittal=`；期次列與缺口說明抽成 `components/ObligationPeriods.jsx`，期限追蹤與履約時程共用。關鍵工項的少量維護在事項詳情（廠商或非正式模式管理者：計畫起迄兩個日期欄、移除；摘要卡「關鍵工項與停留點」有件數與桌機的「加入關鍵工項」搜尋），寫入仍走 store `setItemSchedule`／`removeItemSchedule`（RLS `can_write`），監造／機關唯讀；停留點詳情列允收標準／頻率／出處／掛的工項與計畫起迄／施作事實／查驗並導 `/itp?point=` 或 `/quality?inspection=`。`/schedule` 改為唯讀歷史查閱（無輸入框、無加入／移除；CSV 與手機摘要保留；每列「到履約時程」帶 `?item=<key>`，履約時程換成殼的 `?obligation=wi:<key>` 直達）並在 `navConfig` `hidden: true`（roles 仍只有廠商；深連結依原權限可達）。`item_schedules`／`schedule_periods` 資料不動。驗證見 [BASELINE](docs/BASELINE.md)（`npm test` 131 檔 1,424 項、Demo E2E 8 支 71 項、內建 Preview 1024／375）。**未驗**：正式站真資料（4 案 4 列排程）在履約時程的目視與 `?item=` 直達、真後端由履約時程詳情寫入 `item_schedules` 的實跑；保固類循環義務的保固期滿日來源仍待產品決定。
- **2026-09-19 R1 全面移除兩步驟驗證（MFA／TOTP；D-026 第 7 點 Q1 改決）**：PR #128 已由使用者合併（merge commit `ad8066e`，分支 `codex/slimming-r1-remove-mfa`，單一 commit `7b2f2c9`，rebase 到含 H2／H3、P3a 前端、D1、P5c 的 main `af50930`；auto mode 擋子代理 merge）。PR 檢查 unit／pgtap／e2e／Workers Builds 皆通過（本機 pgTAP 從零套用 73 支 52 檔 2,434；單元 128 檔 1,398；Demo E2E 10 支 74 項；真後端 chain 1／5／6 本機通過且無驗證碼步驟）。同日使用者於自己的終端機 `supabase db push` 套用 `20260919023220_remove_signing_mfa`（子代理未重複執行）；`migration list --linked` 72 筆對齊、無 local-only／remote-only、順序無亂序。正式庫唯讀核對：`auth.mfa_factors` 0 列（verified 0）；`field_document_signatures` 0 列（`platform_account_mfa` 0）；`method` check 唯一一條且為 `platform_account`／`paper_scan`；`sign_field_document` 原始碼不含 `PD003`／`aal2`／`platform_account_mfa` 且寫入 `'platform_account'`；`field_document_signatures_guard` 不含 mfa 分支但仍以 `current_jwt_aal()` 記 `aal`；`authenticated` 可執行 `sign_field_document`、`anon` 不可、guard 函式 `authenticated` 不可執行；guard trigger 啟用；函式註解已標 R1；最後 migration `20260919023220`（共 72 支）。前端：Workers Builds 對 `ad8066e` 的 check-suite `completed success`（11:03:26＋08:00，合併後 77 秒）；正式主 chunk `index-kaND70ox.js` 不含 `/account`／`兩步驟驗證`／`mfaRequired`（R1 前 main 的主 chunk 含 `'/account'`），只剩 supabase-js 函式庫自帶的 `challengeAndVerify`／`getAuthenticatorAssuranceLevel`／`listFactors` 名稱（本機 R1 建置同為 3／3／7，非應用程式碼）；`node scripts/check-prod.js` app 三頁與 demo 兩頁皆 OK；無 Edge 變更；demo 站未重佈。簽署證據自此為：`signer_id`／`signer_org`／`signer_name_snapshot`、伺服器 `signed_at`、`intent` 原文、`version_no`＋`content_hash`（＝版本雜湊）、`request_ip`／`user_agent`、`aal`（如實 aal1，只作證據）。**待使用者**：正式 Supabase Dashboard → Authentication → Multi-Factor 關閉 TOTP 的 Enroll／Verify（`deploy.md` §7；0 個因子，關閉不影響任何帳號）。
- **2026-09-19 P5c 基準日版本（D-026 §4 契約時程與提醒對齊；實作指令 §5／§8「基準日／核准工期變更不破壞歷史」）**：PR #134 已合併（merge commit `ba8f7be`，分支 `codex/slimming-p5c-anchor-versions`，commit `68674b7`＋`614f326`）。PR 檢查 CI／pgTAP 於 `614f326` 皆 success（pgTAP 從零套用 72 支：52 檔 2,436 通過，新增 `project_anchor_versions.sql` 124 條；單元 129 檔 1,403 項；`check:edge` 18 支；`test:edge` 4 項；Demo E2E 8 支 70 項）。同日 `supabase db push` 套用 `20260919021500_project_anchor_versions`（dry-run 只列這一支）；`migration list --linked` 核對本地與遠端 71 筆全部對齊。正式庫唯讀核對（只取計數）：`project_anchor_versions` 12 列＝12 個已填基準日的專案各一版 v1（`initial`、`created_by`／`effective_from` 皆 null，無 v2 以上）；`obligation_periods` 由 8 列變 3 列（`2026-07` 兩案、`2026-08` 一案，皆待辦；竣工後 5 期依界限日移除：一案竣工確認 2026-07-15、另一案報竣 2026-08-21／確認 2026-08-24），3 列皆蓋 `anchor_version_no=1`、無未蓋；7 筆循環義務界限日皆判得出（無停止條件缺口）；`contract_obligations` 兩欄 0 列有值；四支新 trigger 就位、P5b 的 `projects_obligation_periods_sync` 已移除；`update_project_anchors` authenticated 可執行、anon 不可；`project_anchor_versions` RLS 開、一條 select policy、authenticated 只 SELECT、anon 無；pg_cron `pmis-obligation-periods` 仍在。同日以 `deno info` 核對模組圖含 `ballInCourtRules`／`contractDue`／`ballInCourt`／`agentQueryTools` 的四支並以 `--use-api` 從 main `ba8f7be` 重佈：`agent-run` v17→v18、`send-reminders` v19→v20（`verify_jwt=false` 維持）、`fetch-weather` v16→v17、`draft-field-documents` v4→v5；`functions list` 其餘 15 支版本未動（`assistant-chat` v10 不含新模組）。前端隨 main 由 Workers Builds 自動建置，正式主 chunk `index-DMS7lRJl.js` 已含 `update_project_anchors`；`check:prod` app 三頁／demo 兩頁皆 OK；demo 站未重佈。migration `20260919021500_project_anchor_versions`：`project_anchor_versions` append-only（版號、類別 initial／edit／suspension／resumption／extension／change_order、四日期快照、`changed_keys`、生效日、理由、依據函文／變更案、`effects`、建立者；`authenticated` 只 SELECT、UPDATE／DELETE 被 guard 拒、只放行專案刪除 cascade）；`projects` 基準日 INSERT／UPDATE trigger 自動留版並在同交易重算受影響義務（沒動過的待辦期移除／新增／蓋新版號，已提送／已完成／掛證據／待核對的期保留原到期日與依據並記 `kept`；未完成單次義務記改期、已完成記 `kept`），取代 P5b 只補期的 projects trigger；RPC `update_project_anchors`（security definer、第一行以 D-022 `is_project_admin()` 把關＝projects update policy 同一函式；附類別／理由／依據／生效日；四日期未變的停工／復工等仍留版；值沒變回 null）；`contract_obligations` 加 `due_date_snapshot`／`anchor_version_no`（進入已提送／已完成時由 trigger 依當時基準日留快照、退回清空；client 欄位級 grant 不能寫）；循環停止條件：保固類不自動產生，其餘以實際竣工日（`acceptance_events` confirm 優先、否則 report）優先、否則契約竣工日 `end_date` 為界限日，期次只產生到界限日，`acceptance_events` trigger 套用界限並補齊；竣工日缺、或已過而未登錄竣工／展延 → 停止自動產生並列「停止條件待補」；回填：每個已填基準日的專案建 v1（標回填、建立者與生效日 null）、期次 basis 對得上才蓋版號、套用界限日（正式庫盤點：兩案已登錄竣工確認、8 期預計移除竣工後 5 期）；rollback 檔還原 P5b 的 materialize 與 projects trigger。前端／Edge：共用規則 `recurrenceStopGap`／`completionDateOf`／`periodBasisLabel`／`singleDueSnapshot`，待補設定第五種 `stop`（首頁卡／Agent `setup_pending`／早報段，導期限追蹤該筆或驗收頁）；`contractDue.js`／`.ts` 已完成單次義務優先讀快照；Edge 收集器讀 `acceptance_events`、Agent `get_requirements` 帶快照與版號；期限追蹤與履約時程詳情加「依據」列（期次：第 N 版基準日＋起算欄位與日期；單次：完成時留版／未留版／現行第 N 版）與逐期依據句、`stop` 缺口說明；基準日卡加變更類別／依據函文／生效日與 `AnchorVersions`（目前依據哪一版、本版變更與受影響事項、版本紀錄）；改基準日一律經 RPC（store `changeProjectAnchors`），契約價金總額走 `updateProjectSettings`（拒絕基準日鍵）；demo 種子兩版並本地鏡像（期次不重算、標示）。**未驗**：正式站登入後在期限追蹤／履約時程對真資料的「依據」列與版本卡目視、真管理者經 RPC 改基準日的實際留版與重算；保固類循環義務的保固期滿日來源待產品決定（目前不自動產生、列停止條件待補）；rollback 檔未演練。
- **2026-09-19 D1 正式站 Cloudflare 邊緣注入腳本與 `check:prod` 漏檢（D-025 補記）**：PR #133 已合併（merge commit `5a4b955`，分支 `codex/slimming-d1-edge-injection`，commit `e5d8c38`＋`45a0146`）。PR 檢查 CI／pgTAP／Workers Builds preview 與合併後 main（`5a4b955`）的 CI、pgTAP 皆 success。來源：`gov-agent.ai` zone 的 Bot Fight Mode 自動開啟的 JavaScript Detections，對 zone 上所有經 Cloudflare 代理的 HTML 回應（app 含所有 SPA 路由與 `/demo/`、demo 站）在 `</body>` 前塞入行內載入器載 `/cdn-cgi/challenge-platform/scripts/jsd/main.js`；CSP `script-src 'self'` 擋下，每頁一條 console 錯誤；apex 行銷站 DNS-only 不受影響。處置：`public/_headers` 對所有 HTML 回應宣告 `Cache-Control: no-transform`（Cloudflare 對帶此 directive 的回應不注入 JSD／beacon、不壓縮），雜湊資產拆開另設維持 immutable＋壓縮（順帶補上 `/demo/assets/*` 原本缺的 immutable）；`npm run check:prod` 改為允許清單比對（HTML 內每個 `<script>` 須是 repo `index.html` 明示引入者、行內一律紅、`script-src` 恰好 `'self'`、HTML 帶 `no-transform`、入口 chunk immutable 且壓縮），預設檢 app `/`、`/login`、`/demo/` 與 demo `/`、`/login`。demo 站（`pmis-demo`）2026-09-19 已以本分支重佈（同時帶到含 P3a 的 main，Version `15c6fad4…`）：三頁 OK、HTML 無注入、入口 chunk 仍 `br`。**Cloudflare 後台不需操作**：Bot Fight Mode 維持開啟（Free 方案不能單獨關 JSD；它本來就被 CSP 擋、沒拿到訊號，行為不變）；日後若要真的啟用 JSD 必須先修訂 D-025，不得以 nonce 或 `unsafe-inline` 放寬。正式站部署：Workers Builds **沒有**對 `5a4b955` 建置（GitHub 上 Cloudflare 的 check-suite 停在 `queued`、無 build，期間 app 被同一腳本正確報紅 23 分鐘），本單元的 `_headers` 是隨 P5c 的 merge `ba8f7be`（18:24Z）建置上線（正式版本 `5378517c`，18:25Z；核對方法已補進 deploy runbook §2）。部署後 `npm run check:prod` 五頁（app `/`、`/login`、`/demo/`；demo `/`、`/login`）全 OK：app HTML `cache-control: public, max-age=0, must-revalidate, no-transform`、1,618 bytes 無 `content-encoding`、`<script>` 只有 `./theme-boot.js` 與入口 `./assets/index-DMS7lRJl.js`、`challenge-platform` 0 次；入口 chunk 與 `/demo/assets/*` 皆 `public, max-age=31536000, immutable`＋`br`。**未驗**：未在瀏覽器 console 逐頁複核（以 HTML 內容為準）。
- **2026-09-19 P3a 監造日誌頁面（D-026 四類文書之二，前端）**：PR #130 已合併（merge commit `72c044c`，分支 `codex/slimming-p3a-supervisor-log-ui`，commit `5fb0ccb`＋`a8e7bff`＋`198ff44`，rebase 到含 H2／H3 的 main `f8c7891`）。PR 檢查 CI／pgTAP 與合併後 main（`72c044c`）的 CI、pgTAP 皆通過（單元 127 檔 1,372 項；`check:edge` 18 支；`test:edge` 3 項；Demo E2E 10 支 74 項；真後端 chain 6 本機三角色通過，Edge stub 模型）。無 migration、無 Edge 重佈（`_shared/fieldDocText.ts` 只是把既有組字抽成共用模組，`draft-field-documents` 行為不變，正式 v4 未動）。前端由 Workers Builds 隨 main 自動建置：正式主 chunk `index-n1P9w2eA.js`（1.05 MB）已含 `/supervisor-log`、`/supervisor-log/print`、`fn_field_document_template`、`supervisor_log_demo`、`needs_confirmation`，仍無 `saveSiteLog`；`SupervisorLog-DrOQifZM.js`（34 KB）與 `SupervisorLogPrint-DXLF419J.js`（10 KB）含「示範範本」，`Site-CDd-xWU8.js` 含「監造日誌自動起稿」；`npm run check:prod` app／demo 皆 200、CSP `script-src 'self'`、無注入腳本；demo 站未重佈（留給後續一次處理）。內容：`/supervisor-log` 每日一份監造日誌文件頁（監造審核／到場親自確認／簽署／提送機關，機關收件／退回，廠商唯讀；範本標記、必填鍵、人填欄由伺服器 `fn_field_document_template` 推導）、`/supervisor-log/print` 印簽署版本與雜湊；`/site` 監造上傳起稿與文書清單直達；共用元件依 `doc_type` 通用化，施工日誌行為不變（詳 §6.1）。**未驗**：正式站登入後真人走監造日誌三角色流程與真模型辨識品質（P7b）、iPhone 實機、示範站（demo 未重佈，仍為 P1b 版本）。
- **2026-09-19 H2／H3 anon 表級／序列權限與函式 EXECUTE 收回、default privileges 改 fail-closed（H1 回報補列，使用者 2026-09-17 同意執行）**：PR #129 已合併（merge commit `f8c7891`，分支 `codex/slimming-h2h3-anon-privileges`，commit `a347ecd`＋`c6978be`）。PR 檢查 CI／pgTAP 皆通過（pgTAP 從零套用 71 支：51 檔 2,295 通過，新增 `anon_and_function_privileges.sql` 325 條；單元 125 檔 1,357 項；真後端 `e2e:real` auth-smoke／chain1／chain2／chain5 在套用本支的本機棧全綠）。同日 `supabase db push` 套用 `20260919003000_anon_and_function_execute_privileges`（`migration list --linked` 套用前只列這一支待套、套用後本地與遠端 70 筆全部對齊）。正式庫唯讀核對（只取計數）：public 59 個關聯（58 表＋1 view）與 2 個序列 `anon` 零權限、ACL 無 PUBLIC；227 支函式 `anon` 可執行 0、PUBLIC 0、`authenticated` 68（與 pgTAP 允許清單逐名 md5 相同，trigger 函式 0）、`service_role` 227；`pg_default_acl`（postgres）：全域 functions `{postgres=X}`（內建 PUBLIC EXECUTE 已覆蓋）、public functions `{postgres=X,service_role=X}`、public tables `{postgres=arwdDxtm,authenticated=arwd,service_role=arwd}`、public sequences `{postgres=rwU,authenticated=rwU,service_role=rwU}`、extensions functions `{=X}`；`authenticated` 仍可 SELECT `projects`、`service_role` 仍可 INSERT `field_documents`；`projects` 13／`profiles` 21／`project_members` 23／`audit_events` 1,297／`contract_obligations` 109／`field_documents` 2／`obligation_periods` 8 列與套用前相同。Supabase 安全顧問：新增零筆 anon 相關；「signed-in users can execute SECURITY DEFINER」64 筆＝允許清單內的 secdef RPC（皆已審查身分檢查，屬設計）；既有 17 筆 `function_search_path_mutable` 全為 security invoker 純函式（P5b `fn_period_*`、`obligation_party`、`zh_numeral` 等）、`pg_net` in public、兩表 RLS 無 policy（`demo_requests`／`platform_admin_bootstrap`，刻意）、Auth 兩項為既有設定，皆非本支引入。內建 Preview 開正式站 `#/login`、`#/terms`、`#/privacy`、`#/security`：四頁完整呈現、對 `supabase.co` 零請求（登入前沒有 PostgREST 路徑，與盤點一致）；唯一 console 錯誤是 D-025 CSP 擋下 Cloudflare 邊緣注入的 challenge-platform（`jsd`）inline script——不在 repo、`check:prod` 目前只查 `cf-beacon` 而漏掉，另列待辦。`check:prod` app／demo 皆 200、CSP `script-src 'self'`；無 Edge／前端部署；demo 站未重佈。之後每支新 RPC 要在 migration 明示 `grant execute … to authenticated` 並加進 pgTAP 允許清單，否則 pgTAP 紅、上線 42501（DEVELOPMENT §5）；rollback 檔未演練。
- **2026-09-19 P5b 循環義務逐期追蹤（D-026 §4 契約時程與提醒對齊）**：PR #125 已合併（merge commit `5e47cad`，分支 `codex/slimming-p5b-obligation-periods`，commit `621ba06`＋`3aad25e`）。PR 檢查 CI／pgTAP 皆通過（pgTAP 從零套用 70 支：50 檔 1,970 通過，新增 `obligation_periods.sql` 99 條、`requirement_obligation_one_way.sql` 38 條改逐期語意；單元 125 檔 1,357 項；`check:edge` 18 支；`test:edge` 3 項；Demo E2E 8 支 69 項）。同日 `supabase db push` 套用 `20260917233000_obligation_periods`（dry-run 只列這一支）；`migration list --linked` 核對本地與遠端 69 筆全部對齊。正式庫唯讀核對（只取計數）：`contract_obligations` 109 筆、義務層完成 0 筆不變；循環義務 7 筆中 5 筆循環規則不完整（`fn_obligation_recurrence_gap` 非空，三側列「循環規則待補」）、2 筆產生期次共 8 列（期別 `2026-07`～`2026-10`，8 筆待辦、0 完成、0 待核對、2 筆到期日在今天之後＝各含下一期）；pg_cron `pmis-obligation-periods` 一支、排程 `5 16 * * *`；`contract_obligations` 兩支 trigger（sync／guard）與 `projects` 一支就位；`obligation_periods` RLS 開、一條 select policy、`authenticated` 只 SELECT、`anon` 無；`transition_obligation_period`／`materialize_obligation_periods` 可由 `authenticated` 執行，`anon` 與內部 `fn_materialize_obligation_periods` 不可。同日以 `deno info` 核對模組圖含 `ballInCourtRules`／`ballInCourt`／`contractDue`／`agentQueryTools` 的四支 Edge 並以 `--use-api` 從 main `5e47cad` 重佈：`agent-run` v16→v17、`send-reminders` v18→v19（`verify_jwt=false` 維持）、`fetch-weather` v15→v16、`draft-field-documents` v3→v4（後兩支因 `contractDue.ts` 改 import 共用規則而進模組圖）；`functions list` 其餘 15 支版本未動（`assistant-chat` v10 不含新模組）。前端隨 main 由 Workers Builds 自動建置，正式主 chunk `index-BEZXz8WO.js` 已含 `obligation_periods`／`transition_obligation_period`／「循環規則待補」／「回填待核對」；`check:prod` app／demo 皆 200、CSP `script-src 'self'`、無注入腳本；demo 站未重佈。migration `20260917233000_obligation_periods`：`obligation_periods` 每義務每期一列（期別鍵 daily `YYYY-MM-DD`／weekly `IYYY-Www`／monthly `YYYY-MM`／quarterly `YYYY-Qn`／yearly `YYYY`、期間起訖、到期日、狀態、完成時間／人、送審／現場文書證據、`review_note`、`basis`、`anchor_version_no` 預留 P5c）；RLS 以子查詢沿用義務可見性、`authenticated` 只有 SELECT；純函式期次排程（月末夾住、閏年、跨年、季／年／週／日、列到今天＋31 日且永遠含下一期）；冪等 materialize（`on conflict do nothing`，只補不改）由義務插入／規則變更／廢止 trigger、`projects` 四個基準日變更 trigger、pg_cron `pmis-obligation-periods`（每日 16:05 UTC＝台北 00:05，純 SQL、不需 secret；正式庫 pg_cron 已啟用）與成員／service 可呼叫的 RPC `materialize_obligation_periods(project)` 觸發；起算基準日＝觸發點對應日期（fixed 用義務自己的日期），無觸發點一律開工日；基準日缺或循環規則不完整（每月缺幾日等）不產生期次、列「待補設定」；期次狀態只經 RPC `transition_obligation_period`（歸屬規則同義務 update policy、證據須同案、伺服器蓋完成時間、退回待辦解除證據）；`contract_obligations` 對循環義務加 guard，義務層不可再標已提送／已完成（舊前端／直接 REST 明確失敗）；回填：既有循環義務全部產生期次，義務層曾標完成者有 `completed_at` 就對應期別、推不出則原狀不動並在已到期的待辦期次標 `review_note`。前端／Edge：共用規則 `obligationEntries` 對每個未結期次各出一顆球（鍵 `契約重點:<id>:<期別>`、深連結 `/deadlines?obligation=<id>&period=<期別>`）；`contractDue.js`／`.ts` 循環義務改讀 `ob.periods` 最早未結一期，不再從今天推算；`loadObligationsFromDB`／Edge 收集器／`get_requirements` 以 PostgREST embed 載期次；期限追蹤逐期標記（`?period=`）、履約時程詳情唯讀列期次；待補設定新增 `rule`／`review` 兩種缺口；demo 種子三筆 monthly 帶上一期已完成＋下一期待辦。**未驗**：正式 7 筆 monthly（5 筆循環規則待補、2 筆期次）在首頁／Agent／早報／期限追蹤的真資料目視；pg_cron 首次執行紀錄（`cron.job_run_details`，首跑 2026-09-19 16:05 UTC）；期限追蹤逐期標記在真後端；rollback 檔未演練。
- **2026-09-17 P3a 監造日誌後端（D-026 四類文書之二）**：PR #122 已合併（merge commit `ba7c596`，分支 `codex/slimming-p3a-supervisor-log-backend`）。PR 檢查 CI／pgTAP 皆通過（pgTAP 從零套用 68 支：48 檔 1,859 通過，新增 `supervisor_logs.sql` 128 條，`field_document_sign.sql` 140 條回歸；單元 124 檔 1,342 項；`check:edge` 18 支；`test:edge` 3 項）。同日 `supabase db push` 套用 `20260917221000_supervisor_logs`；`migration list --linked` 核對本地與遠端 68 筆全部對齊。正式庫唯讀核對：`supervisor_logs` 就位、RLS 開、四條 policy（select／insert／update／delete）、`(project_id, log_date)` 唯一、`supervisor_logs_guard`／`daily_logs_guard`／`daily_log_items_guard` 三支 trigger 掛上、`authenticated` 只有 SELECT／INSERT／UPDATE／DELETE（anon 無任何表級權限，TRUNCATE／REFERENCES／TRIGGER 殘留 0）；11 支新函式就位而 P2d 的 `fn_daily_log_signed`／`fn_daily_log_sign_bypass` 與二參數 `fn_field_document_unmet_fields` 已移除；`authenticated` 可執行 `fn_field_document_template`／`save_field_document_version`／`sign_field_document`，不可執行人填欄推導、簽署分支內部函式與通用 guard，anon 不可取範本；範本 `supervisor_log_demo` v1 `is_demo=true`、`demo_label='示範範本'`、免責聲明就位；監造日誌必填鍵＝`attendance`／`contractor_summary`／`log_date`／`supervision_items`／`weather_am`／`weather_pm`、人填欄＝`attendance`、施工日誌必填六欄不變；`supervisor_logs` 0 列、`field_documents` 0 列、既有 12 筆 `daily_logs` 0 筆簽署——對現有作業零行為影響。同日以 `--use-api` 重佈 `draft-field-documents` v1→v2（`verify_jwt=true`；只有這支的模組圖含 `fieldDoc*`，其餘 18 支版本未動）；`check:prod` app／demo 皆 200、CSP `script-src 'self'`、無注入腳本。內容：`supervisor_logs` 事實表（每案每日唯一；監造寫、專案成員可讀、非成員與跨案不可）；到場只能人填（AI／service 版本帶入值或標 filled／confirmed／na 即拒、人工 filled 只算待確認 `needs_confirmation`、簽署須 confirmed 或 na＋reason）；`sign_field_document` 改共用前段＋依類型分派到內部函式（`daily_log` 行為不變；`supervisor_log` 驗到場／工項／查驗／缺失／收件情形引用皆本案後落 `supervisor_logs`、綁 `target_id`；`self_check`／`inspection_form` 仍 PD007）；事實表 guard 規則單一實作 `fn_field_document_fact_guard`；Edge 監造批次真正起監造日誌（監造事項只從監造照片與當日查驗整理、查驗／通知／追蹤只帶系統紀錄並標來源、廠商施工情形與天氣只引用已簽署／提送的施工日誌文件、到場永遠留空 pending）；球權待辦由 P5a `ballInCourtRules.ts` 涵蓋。**未驗／未做**：監造日誌頁面（待 P2c 共用審核／簽署元件）；正式站尚無前端對 `draft-field-documents` 發請求；模型品質仍以 stub 證明流程（P7b）；rollback 檔未演練。
- **2026-09-19 P2c 現場紀錄頁：第一條完整路徑（D-026）**：PR #124 已合併（merge commit `bb5f34a`，分支 `codex/slimming-p2c-site-flow`，rebase 到含 P3a 的 main `4410cee`）。PR 檢查 CI／pgTAP／e2e／Workers Builds 與合併後 main 的 CI、pgTAP 皆通過（單元 125 檔 1,354 項；pgTAP 從零套用 68 支 49 檔 1,864，新增 `service_role_function_grants.sql` 5 條；Demo E2E 7 支 63 項；真後端 chain 5 本機三角色通過，本機 Edge stub 模型）。無 migration；`supabase/config.toml`（本機 TOTP）與 `seed.sql`（本機 service_role 函式 EXECUTE 鏡像 hosted 平台預設）只影響本機 stack。同日以 `--use-api` 從 main `bb5f34a` 重佈 `draft-field-documents`：`functions list` 核對 v2→v3、`verify_jwt=true`，其餘 18 支版本未動（index.ts 接本機 stub，正式 `SUPABASE_URL` 為 https 故永遠關閉）；前端由 Workers Builds 自動建置，正式主 chunk `index-DpEdh4Nn.js` 已含 `photo_intakes`／`draft-field-documents` 且無 `saveSiteLog`，`Site-Cj_6zwYR.js` 含「拍照／上傳」；`npm run check:prod` app／demo 皆 200、CSP `script-src 'self'`、無注入腳本；demo 站未重佈。**未驗**：正式站登入後對 `draft-field-documents` 的真實請求與真模型辨識品質（P7b）、iPhone 實機三流程、真人三角色驗收。
- **2026-09-17 P5a 球權單一實作與待補設定（D-026 §4 契約時程與提醒對齊）**：PR #120 已合併（merge commit `1edd3c8`，分支 `codex/slimming-p5a-ball-in-court`）。PR 檢查 CI／pgTAP 皆通過（pgTAP 從零套用 67 支：47 檔 1,728 通過，新增 `obligation_party_unassigned.sql` 16 條；單元 124 檔 1,331 項；`check:edge` 18 支；新 `npm run test:edge` 3 項＝Deno 執行期跑同一組共用案例，已進 CI）。同日 `supabase db push` 套用 `20260917220737_obligation_party_unassigned`；`migration list --linked` 核對本地與遠端 67 筆全部對齊。正式庫唯讀核對：`obligation_party` 對 null／空字串／「其他」／未知文字回 null、`' 機關 '` 回機關、`廠商` 原樣、IMMUTABLE、函式註解就位；`contract_obligations_update` policy 條件與套用前相同（`admin_override or obligation_party(responsible) = my_party()`）；109 筆義務中 7 筆責任不明（皆 `待辦`）——自此三方皆不可標記、在首頁／Agent／早報列為待補設定。同日以 `--use-api` 重佈模組圖含新模組的兩支 Edge（`deno info` 核對；`assistant-chat` 不含）：`agent-run` v15→v16、`send-reminders` v17→v18（`verify_jwt=false` 維持）；`functions list` 其餘 17 支版本未動。前端隨 main 由 Workers Builds 自動建置，`check:prod` app／demo 皆 200、CSP `script-src 'self'`、無注入腳本。內容：首頁今日工作、Agent `list_my_open_items` 與早報 `send-reminders` 改 import 同一支 `_shared/ballInCourtRules.ts`（七支單據＋現場文書＋契約義務責任／基準日缺口），共用案例 `tests/fixtures/ball-in-court.cases.json` 三側斷言相同事項、責任與期限；Edge 補查驗／變更／觀察／現場文書；責任推不出三方或基準日沒填的事項不歸任何一方，改列「待補設定」（首頁一張卡、Agent 回 `setup_pending`、早報一段不觸發寄信）。migration `20260917220737_obligation_party_unassigned`：`obligation_party()` 對三方以外回 null，既有 update policy 因此對三方都不放行（只剩非正式模式 admin override），不改任何資料列；前端 `obligationParty` 同步回「待補設定」。合併後需：`supabase db push`、`--use-api` 重佈 `agent-run`／`assistant-chat`／`send-reminders`、`check:prod`。真資料目視（demo 沒有責任不明的義務）與 `/site?doc=` 直達（待 P2c）未驗。
- **2026-09-17 H1 收回 API 角色對 public 表的 TRUNCATE／REFERENCES／TRIGGER／MAINTAIN（P1b 發現，D-026 瘦身續接）**：PR #118 已合併（merge commit `dfc3953`，分支 `codex/slimming-h1-table-privileges`）。PR 檢查 CI／pgTAP 皆通過（pgTAP 從零套用 66 支：46 檔 1,712 通過，新增 `api_roles_table_privileges.sql` 194 條＝57 關聯 × 三角色迴圈＋23 條；單元 122 檔 1,301 項；真後端 e2e:real chain1／chain2 在套用 H1 的本機棧全綠）。同日 `supabase db push` 套用 `20260917213900_api_roles_table_ddl_privileges`（只改 ACL：`revoke truncate, references, trigger, maintain on all tables in schema public from public, anon, authenticated, service_role`＋`alter default privileges for role postgres in schema public revoke` 同四種；rollback 檔同名 `.down.sql`）；`migration list --linked` 核對本地與遠端 66 筆全部對齊。正式庫唯讀核對：57 個 public 關聯對 `anon`／`authenticated`／`service_role` 四種權限殘留 0（套用前 anon／authenticated 各 50、service_role 57）；SELECT／INSERT／UPDATE／DELETE 計數與套用前完全相同（anon 49／39／39／40、authenticated 54／38／38／40、service_role 57 全有）；`pg_default_acl` 的 `postgres`／`public`／tables 由 `arwdDxtm` 變為 `anon=arwd,authenticated=arwd,service_role=arwd`；`cost_items` 11 列／4 案、`audit_events` 1,290 列、56 表 RLS 全開皆無變。不含 Edge／前端變更；本機 `seed.sql` 同步改為只給 service_role DML。**未動**：正式庫 default ACL 仍給 `anon` 表級 DML 與序列 UPDATE、給三角色新函式 EXECUTE（本機 secure-by-default 沒有），列續接清單 H2／H3 候選待使用者決定；rollback 檔未演練。
- **2026-09-17 P2b Edge 起稿 `draft-field-documents`（D-026）**：PR #116 已合併（merge commit `b52cd25`，分支 `codex/slimming-p2b-draft-edge`）。PR 檢查 CI／pgTAP 皆通過（單元 122 檔 1,301 項；pgTAP 從零套用 45 檔 1,518 通過，新增 `ai_field_docs_draft.sql` 8 條；`check:edge` 18 支）。同日 `supabase db push` 套用 `20260917213500_ai_field_docs_draft`（只 seed `ai_features` 一列 `field_docs.draft`：draft 類、`draft-field-documents`、trial、LLM、預設開啟、sort_order 105；rollback 檔關閉開關不刪列）；`migration list --linked` 核對本地與遠端 65 筆全部對齊；正式庫唯讀核對該列七欄如 seed，`ai_features` 共 18 列、16 列開啟（退場的 assistant.chat／contract.parse 仍關閉）。同日以 `--use-api` 從 main（`b52cd25`）重佈全部 18 支 Edge（`_shared/aiGate.ts` 改為 `askAiFeature` 單一判定點、新 `_shared/sitePhotoVision.ts` 供 classify-site-photo／read-whiteboard／起稿三處共用，依 runbook 重佈所有 import `_shared/` 的函式）：`functions list` 核對既有 17 支版本均 +1（agent-run 15、extract-requirements 14、classify-site-photo 11、read-whiteboard 15、send-reminders 17 且 `verify_jwt=false` 維持），新增 `draft-field-documents` 版本 1（`verify_jwt=true`）。前端只有 `src/lib/photoMatch.js` 改為 re-export `_shared/photoMatch.ts`（行為不變），隨 main 由 Workers Builds 自動建置，合併後 `npm run check:prod` app／demo 皆 200、CSP `script-src 'self'`、無注入腳本。**未驗**：`draft-field-documents` 尚無前端呼叫端（P2c），正式站未對它發過請求；模型辨識品質只以 stub 證明流程、未以真照片實跑（列 P7b）。

- **2026-09-17 瘦身 P1b 退場頁唯讀化（D-026 §4／§5）**：PR #113 已合併（merge commit `96efb56`，分支 `codex/slimming-p1b-retire-readonly`）。PR 檢查 CI／pgTAP 皆通過（pgTAP 從零套用 64 支：44 檔 1,510 通過，新增 `cost_items_retired.sql` 24 條；單元 120 檔 1,261 項；Demo E2E 六檔 56 項）。部署順序依 runbook「收緊型先前端後 DB」：合併後 Workers Builds 自動建置正式站（主 chunk `index-BPR2IHOP.js`，`Cost-CiEHm--M`／`Valuation-Haw1CWBB`／`Portfolio-Dw60AoO0`／`RiskAudit-D2TfS_FN` 各 chunk 已含「歷史查閱」「本期勾稽檢核」「專案清單」「唯讀查閱」文案），核對後同日 `supabase db push` 套用 `20260917210000_cost_items_retire`（`migration list --linked` 本地與遠端 64 筆全部對齊）。正式庫唯讀核對：`cost_items` 只剩一條 policy `cost_items_contractor_read`（SELECT）；`anon`／`authenticated` 表級權限剩 SELECT（INSERT／UPDATE／DELETE 已收回；`service_role` 保留全部供修復），既有 11 列／4 案完整無損。同日以本 worktree 重佈 demo 站（`VITE_SUPABASE_URL=` 建置，`wrangler deploy --config wrangler.demo.jsonc`，Version `3afb3109`，bundle 不含 Supabase 網址），`npm run check:prod` app／demo（含 workers.dev 備援）皆 200、CSP `script-src 'self'`、無注入腳本；demo 各頁 chunk 同樣已含新文案、無「新增成本／各案均無未結例外／今日待辦」舊字串。不含 Edge 變更；正式站登入後流程與真人驗收未核對；`authenticated` 對所有 public 表仍保有 Supabase 預設的 REFERENCES／TRIGGER／TRUNCATE（非本單元引入、RLS 不涵蓋 TRUNCATE，PostgREST 不暴露），已由 H1 `20260917213900` 收回。
- **2026-09-17 P2d 施工日誌存版／簽署／提送 RPC 與事實表 guard（D-026）**：PR #112 已合併（merge commit `2f9f68d`，分支 `codex/slimming-p2d-sign-submit`）。PR 檢查 CI／pgTAP 皆通過（pgTAP 從零套用 43 檔 1,486 通過，新增 `field_document_sign.sql` 140 條，全走真實 `authenticated`＋JWT 路徑；單元 120 檔 1,259 項）。同日 `supabase db push` 套用 `20260917205000_field_document_rpcs`：新增 `save_field_document_version`（四類共用；必填鍵／待補由伺服器算並寫回）、`sign_field_document`（只有 `daily_log` 分支，其他類型回 `PD007`；aal2 政策、必填完整性、附件角色隔離、落 `daily_logs`／`daily_log_items`、綁 `target_id`、`agent_actions` 內部處理）、`submit`／`receive`／`return_field_document`（`client_request_id` 冪等）、`daily_logs_guard`／`daily_log_items_guard`（已簽署列只有簽署 RPC 交易內可重寫，DELETE 一律擋；未簽署列照舊）、`field_documents_target_uidx` 改只算活文件；錯誤代碼 SQLSTATE `PD001–PD010`；`migration list --linked` 核對本地與遠端 63 筆全部對齊。正式庫唯讀核對：五支公開 RPC 皆 `security definer`、`search_path=public`、只有 `authenticated` 可執行（anon 不可）；`resolve_agent_action_internal`／`field_document_respond_internal`／兩支 guard／六支 helper `authenticated` 不可執行；兩支 guard trigger 已啟用；索引定義含 `status <> ALL (discarded, superseded)`；既有 12 筆 `daily_logs`（33 筆明細）無任何簽署文件指向、0 筆 `已簽署`，`field_documents`／簽署／提送皆 0 筆——對現有日誌作業零行為影響（舊 `saveSiteLog` 只在該日已有簽署文件時才會收到 P0001）。不含 Edge 或前端變更；P2b／P2c 之前這些 RPC 沒有任何呼叫端。
- **2026-09-17 P2a 現場文書家族資料層（D-026）**：PR #109 已合併（merge commit `3f2a00a`，分支 `codex/slimming-p2a-field-docs`）。PR 檢查 CI／pgTAP 皆通過（pgTAP 從零套用 42 檔 1,346 通過，新增 `field_documents.sql` 215 條；單元 120 檔 1,259 項）。同日 `supabase db push` 套用 `20260917201000_field_documents`：新增 `photo_intakes`、`field_documents`／`_versions`／`_signatures`／`_submissions` 五表（RLS＋欄位級 grants＋六支 guard＋四支稽核 trigger）、`photos` 加七欄與 `photos_org_stamp`（上傳方由伺服器決定）、`photos` 的 insert／update 改欄位級 grant（既有欄全保留、AI 欄只有 service 可寫）；`migration list --linked` 核對本地與遠端 62 筆全部對齊。正式庫唯讀核對：5 表、10 支 trigger、9 條 policy 就位；authenticated 對 `field_documents` 無 UPDATE、只有 `doc_type` 等六欄的 INSERT、對版本／簽署／提送表無任何寫入；`photos` 的 `uploader_org`／`ai_status` 客戶端不可寫、`caption` 仍可改；anon 不可讀；既有 2 筆照片的 `uploader_org` 皆由上傳者 profile 回填、0 筆未知。不含 Edge 或前端功能（`auditEvents.js` 只加標籤）；P2b／P2c／P2d 之前這些表沒有任何寫入端，對現有流程零行為影響。
- **2026-09-17 P4a 監造確認量純計算層（D-026）**：PR #103 已合併（merge commit `591570c`，分支 `codex/slimming-p4a-calc`）。PR 檢查 CI／pgTAP 皆通過（pgTAP 從零套用 41 檔 1,131 通過，新增 `confirmed_quantity_calc.sql` 83 條）。同日 `supabase db push` 套用 `20260917120000_confirmed_quantity_calc`（只新增 2 型別＋14 支 IMMUTABLE／security invoker 純函式，不動任何表、trigger、policy 或資料列）；`migration list --linked` 核對本地與遠端 61 筆全部對齊；正式庫唯讀核對 14 支函式皆 IMMUTABLE、security invoker，anon／authenticated 均不可執行。不含 Edge 或前端變更；P4b 之前這些函式沒有任何呼叫端，對現有流程零行為影響。使用者同日對續接清單 §6 的答覆記入 D-026 第 7 點。
- **2026-09-17 瘦身 P1c＋P1d 文案對齊四主入口與手機抽屜斷點缺陷（D-026）**：PR #108 已合併（merge commit `f6ba60c`，分支 `codex/slimming-p1cd`）。PR 檢查與合併後 main 的 CI、pgTAP 皆通過（120 檔 1,263 項單元；Demo E2E 含新增抽屜斷點測試）；前端由 Workers Builds 自動建置，合併後 `npm run check:prod` app／demo 皆 200、CSP `script-src 'self'`、無注入腳本，正式主 chunk 已不含舊「常用工作」文案。純前端文案與 Layout 抽屜狀態推導＋文件，不含 DB migration 或 Edge 部署；demo 站未重佈（待 P1b 後一次重佈）；正式站登入後流程與真人驗收未核對。
- **2026-09-17 瘦身 P1a 導覽重組（D-026 四主入口）**：PR #104 已合併（merge commit `33d0f70`，分支 `codex/slimming-p1a-nav`）。PR 檢查與合併後 main 的 CI、pgTAP 皆通過（120 檔 1,255 項單元、10 支 71 項 Demo E2E）；前端由 Workers Builds 自動建置，合併後 `npm run check:prod` app／demo 皆 200、CSP `script-src 'self'`、無注入腳本，正式站 bundle 已含新導覽（現場總覽／文件往來）。純前端，不含 DB migration 或 Edge 部署；demo 站未重佈（仍為舊導覽）；正式站登入後流程與真人驗收未核對。
- **2026-09-16 補強包 A（Codex 實測 W01–W04）**：PR #88 已合併（merge commit `2fb555a`，分支 `fix/uiux-package-a`）。PR 檢查 unit／e2e／pgtap／Workers Builds 與合併後 main 的 CI、pgTAP 皆通過（116 檔 1,226 項單元、52 項相關 Demo E2E）；前端由 Cloudflare 自動建置，不含 DB migration 或 Edge 部署；正式站登入後流程與真人驗收未核對。Codex 報告已隨 PR #87 入庫。
- **2026-09-16 補強包 B（Codex 實測 W05／W06／W08／D01）**：PR #90 已合併（merge commit `fe0e1bd`，分支 `fix/uiux-package-b`）。PR 檢查 unit／e2e／pgtap／Workers Builds 與合併後 main 的 CI、pgTAP 皆通過（116 檔 1,228 項單元、55 項相關 Demo E2E）；前端由 Cloudflare 自動建置，不含 DB migration 或 Edge 部署；正式站登入後流程與真人驗收未核對。
- **2026-09-16 補強包 C（Codex 實測 W07／W09）**：PR #92 已合併（merge commit `e54d39a`，分支 `fix/uiux-package-c`）。PR 檢查 unit／e2e／pgtap／Workers Builds 與合併後 main 的 CI、pgTAP 皆通過（116 檔 1,228 項單元、45 項相關 Demo E2E）；內容為 W09 文案與 W07 口徑對照文件，不含進度公式、DB migration 或 Edge 部署；W07 標示文案待 C1 決策後另行實作。
- **2026-09-16 補強包 C 第二批（進度口徑 D-024）**：PR #94 已合併（merge commit `11846ca`，分支 `fix/uiux-package-c2`）。PR 檢查 unit／e2e／pgtap／Workers Builds 與合併後 main 的 CI、pgTAP 皆通過（118 檔 1,239 項單元、60 項相關 Demo E2E）；前端由 Cloudflare 自動建置，不含 DB migration 或 Edge 部署（`portfolio_summary` 未改）；正式站登入後流程與真人驗收未核對。
- **2026-09-16 小包 D（Codex §5 版面與操作感受）**：PR #96 已合併（merge commit `0d9cce1`，分支 `fix/uiux-package-d`）。PR 檢查 unit／e2e／pgtap／Workers Builds 與合併後 main 的 CI、pgTAP 皆通過（119 檔 1,241 項單元、59 項相關 Demo E2E）；純前端版面與文案，不含 DB migration 或 Edge 部署；正式站登入後流程與真人驗收未核對。
- **2026-09-16 線上 demo 站**：新增 Cloudflare Worker `pmis-demo`（Free 方案靜態資產，使用者核准），網址 `https://pmis-demo.ryanxhuang1212.workers.dev`，以 main `1d15b42` 用 `VITE_SUPABASE_URL=`、`VITE_SUPABASE_ANON_KEY=`、`VITE_SENTRY_DSN=` 建置後 `wrangler deploy --name pmis-demo`（Version `1678943e`）。已核對：登入頁出現三角色鈕、監造登入落首頁、無 console 錯誤、bundle 不含 Supabase 網址。不隨 main 自動更新，重佈指令見 DEVELOPMENT。同日綁自訂網域 `https://demo.gov-agent.ai`（`wrangler.demo.jsonc` 的 `routes[].custom_domain`，Version `e70fbfdc`），已核對 200、登入頁三角色鈕、無 console 錯誤；workers.dev 網址保留為備援。
- **2026-09-16 Cloudflare Web Analytics 改手動載入（D-025）**：`gov-agent.ai` zone 的站設定由「Enable, excluding EU」改為「Enable with JS Snippet installation」，邊緣不再對任何主機注入 beacon；`npm run check:prod` 核對 app 與 demo 皆 200、CSP `script-src 'self'`、HTML 無 `cf-beacon`。行銷站（`PMIS.marketing`）改由 `Base.astro` 明示載入 beacon 並加 meta CSP，隨 GitHub Pages 部署。
- **2026-09-15 UIUX 階段 1–6（桌機三角色流程）**：PR #85 已合併（merge commit `afdaefb`，分支 `ui/uiux-stages-1-6`）。PR 檢查 unit／e2e／pgtap／Workers Builds 與合併後 main 的 CI、pgTAP 皆通過（114 檔 1,218 項單元、10 檔 70 項 Demo E2E、lint／build／check:docs）；前端由 Cloudflare 自動建置，建置版本以 Cloudflare 後台為準。前端變更，不含 DB migration 或 Edge 部署；正式站登入後流程與真人驗收未核對。
- **2026-09-14 UIUX 工作包**：已隨 PR #83 合併（`7156aef`）。前端變更，不含 DB migration 或 Edge 部署。

- **DB**：2026-09-17 `supabase db push` 先後套用 `20260917120000_confirmed_quantity_calc`（P4a 純函式）、`20260917201000_field_documents`（P2a 現場文書資料層）、`20260917205000_field_document_rpcs`（P2d，由該單元套用）、`20260917210000_cost_items_retire`（P1b 成本寫入收回）、`20260917213500_ai_field_docs_draft`（P2b AI 功能 seed）、 `20260917213900_api_roles_table_ddl_privileges`（H1 API 角色表級 DDL／維護類權限收回＋default privileges）與 `20260917221000_supervisor_logs`（P3a 監造日誌後端：`supervisor_logs` 事實表、示範範本、通用事實表 guard、`supervisor_log` 簽署分支）；2026-09-19 再套用 `20260917233000_obligation_periods`（P5b 期次表）與 `20260919003000_anon_and_function_execute_privileges`（H2／H3 anon 零權限、authenticated 函式允許清單、default privileges fail-closed）；`migration list --linked` 核對本地與遠端 70 筆全部對齊，最新 `20260919003000`；2026-09-19 P5c 再套用 `20260919021500_project_anchor_versions`（基準日版本、期次界限日、單次義務到期快照；回填 12 版 v1、8 期依界限日剩 3 期），71 筆對齊、最新 `20260919021500`。前一次 2026-09-11 套用 `20260911100000_demo_requests_revoke_grants`、`20260911100100_contract_parse_retire`、`20260911110000_project_admin_single_source`。
- **Edge**：2026-09-17 以 `--use-api` 從 main（`b52cd25`，P2b）重佈全部 18 支；`functions list` 核對既有 17 支版本均 +1（agent-run 15、extract-requirements 14、send-reminders 17 且 `verify_jwt=false`、classify-document 4 等），新增 `draft-field-documents` 版本 1；同日 P5a 重佈 `agent-run` v16／`send-reminders` v18（`verify_jwt=false` 維持），P3a 重佈 `draft-field-documents` v1→v2（`verify_jwt=true`；只有這支的模組圖含 `fieldDoc*`）。2026-09-19 P5b／P5c 各重佈模組圖含共用規則的四支（P5c 後 `agent-run` v18、`send-reminders` v20、`fetch-weather` v17、`draft-field-documents` v5）。前一次 2026-09-11 從 main 重佈 17 支。線上另有 `demo-request` 一支由行銷站 repo 部署，不在本 repo。
- **前端**：main 每次合併由 Cloudflare Workers 自動建置。2026-09-11 PR #64（`8be082a`）建置版本 `fc99c933-32f5-47c6-b0e6-726e50b2956e`，首頁 HEAD 200、七項安全標頭齊全；同日 PR #67／#69／#70／#76／#78 及 2026-09-12 PR #79 陸續合併，各次建置版本未逐一記錄，以 Cloudflare 後台為準。這不代表登入後業務流程或正式後端已驗證。
- **舊站**：2026-09-11 GitHub Pages API 仍回 built，來源為 `gh-pages`；此部署分支保留。`pmis.pages.dev` 最後核對為 2026-09-07，退場待另行處理。
- 部署依 [runbook](docs/operations/deploy.md) 執行；套用後在本節記日期、migration／Edge 版本與驗證。2026-09-11 已完成三面同步：前端由 main 自動部署、DB 三支 migration 套用、17 支 Edge 重佈；登入後業務流程與真模型抽取仍未在正式站實測。

### 6.4 主要機制

- 建案後進專案文件。Dashboard 初始化為五步（含開工日）；AI 步驟只看存在 completed run，正式模式不因三方未齊而鎖住。D-014 舊四步條文仍待正式修訂。
- 標單匯入／重設走單一交易 RPC；真正專案與已載入 DB 標單是兩種模式，不能將 Demo 工項寫入真專案。
- `/agent` 是完整 AI 入口，Copilot 是薄入口，`/assistant` 導向 `/agent`。DB `ai_features` 為執行期權威，前端／Edge 各有鏡像。
- 文件：上傳 → 確定性分類（AI 第二意見）→ 契約包 → 抽取。原始檔凍結，私有 bucket，讀取留痕失敗時拒絕；刪檔走 `delete_document` RPC。processing／ingestion 分別有 20／10 分鐘過期時鐘。
- 抽取可跨 request 續跑，逐頁檢查 exact count／連續頁序／page_count；缺頁不開始抽取，無文字／無效輸出／截斷揭露部分完成，partial 契約包不算 ready。24 批上限、OCR 與語意漏抽未解。
- `/requirements` 為三方履約時程，`/requirements/review` 為擷取審核，`/deadlines` 管期限／罰則／基準日。D-018 RLS 保護契約分級；可見範圍的前端 `VISIBLE` shim 仍保留，動作依歸屬。
- 照片辨識只回填日誌草稿，仍須人存檔；查驗可檢附自檢表；試體 28 天不合格與開缺失同交易並去重。`audit_events` append-only。

### 6.5 資料存取

跨頁資料由 `store.jsx`／slices 管理；單頁有界資料可直接查 Supabase。目前直接查詢頁為 Contract、Requirements、RequirementsReview、Activity、Dashboard。契約共用查詢配方在 `useContractEnrichment`，不是共用狀態。重複計算／查詢放 lib；業務日期用 `dates.js`，金額格式用 `format.js`。

API 角色權限（自 migration `20260919003000`，H2／H3）：`anon` 對 public 的表／view／序列／函式一律零權限——登入前只有 GoTrue（登入／註冊／密碼重設／MFA）與四條靜態公開路由，沒有任何 PostgREST 路徑；`authenticated` 只能執行明示允許清單的函式（前端／Edge user client 的 RPC、RLS 與 Storage policy 引用的 helper、欄位 default、既有明示 grant；trigger 函式與內部 helper 不可直接呼叫），清單由 pgTAP `anon_and_function_privileges.sql` 精確釘住；`service_role` 維持平台預設（伺服器端信任邊界，繞過 RLS）。default privileges：新表仍自動帶 `authenticated` DML（migration 內收窄），新函式對 `anon`／`authenticated`／PUBLIC 不可執行（fail-closed，要用就明示 grant）。全庫 security definer 函式 `search_path` 皆固定，authenticated 可呼叫者都以 `auth.uid()`／成員資格／`is_platform_admin` 檢查身分（`ai_feature_allowed` 只回開關布林，成員資格由 Edge 閘門另證）。

## 7. 仍存在的限制

- 前端／Edge 的待辦類型自 P5a 起單一實作，剩餘差異只有呼叫端的到期窗口參數；循環履約自 P5b 逐期、P5c 起有基準日版本與停止條件，但保固類循環義務沒有保固期滿日欄位可判定（不自動產生、列停止條件待補），逐期準時率自 P5d 起在履約時程的期次區與執行卡呈現（以期計）。詳見 [雙引擎](docs/architecture/dual-engine-sync.md)。
- 正式且已匯標單的專案是否缺一般成員頁入口，待實測確認。
- obligation runtime、雙成員相容欄位與 `VISIBLE` shim 仍有使用端，不可直接刪除。
- rollback 檔僅覆蓋少數，存在檔案不代表回復演練通過。部分領域狀態欄無 CHECK，processing run 無轉移 guard。
- 唯讀 Agent 呼叫軌跡不落庫。其餘已知缺口與待決事項集中 [ROADMAP](docs/ROADMAP.md)。

## 8. 文件權威

流程看 DEVELOPMENT；現況看本檔與程式／migration；目標看 DECISIONS／ACTIVE 架構；續接看 ROADMAP；測試看 BASELINE。UI 規範與長期北極星按任務讀。過期報告／handoff 已移除，原文可用 `git show c39e395:<路徑>` 追溯；不再當現行規格。
