node_modules
dist
.env
.env.local
.env.e2e.real
.env*.save
.DS_Store
.playwright-mcp

# Playwright E2E 產物
test-results/
playwright-report/

# Supabase CLI local state (linked project ref 等本機狀態，勿入版控)
supabase/.temp/

# 暫存/QA 測試 fixtures 與 scratch 產物，勿入版控
tmp/

# 發包圖說原始檔（657MB，勿入版控；標單已解析成 src/data/workItems.json）
發包圖說20200715/
*.output
*-check.png
*-ok.png
mobile-home.png
self-inspection.png
發包圖說*.zip
.wrangler/

# git worktree 殘留(本機 305MB)。原本只寫在 .git/info/exclude,那份不入版控 ——
# 換一台機器 clone 後只要有人開 worktree,整包就會裸露在 git status。
.claude/worktrees/

# 簡報成品與中間產物:都可由 docs/pitch/pptx/ 的建置腳本重建,不入版控。
# 腳本本身、brand/ 標誌與 shots/ 來源截圖則是常備資產,要追蹤。
docs/pitch/*.pptx
docs/pitch/*.pdf
docs/pitch/shots-crop/
docs/pitch/_to_delete/
UIUX/*.zip

# 公司營運文件(非產品文件),留在本機不入版控
docs/青創基地/

# 行銷站產品畫面:由 scripts/capture-appshots.mjs 從 demo 建置重截,可重建的產物;
# 入版控的是行銷站 repo 那份(消費端),這裡只留 README。
UIUX/appshots/*.png
